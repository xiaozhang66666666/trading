# 策略假设与取舍

## 1) `ma_cross`（均线趋势）
- 核心规则：快均线 > 慢均线做多，快均线 < 慢均线做空，否则空仓。
- 假设：价格存在中长期趋势，均线交叉可近似判定 regime。
- 适用场景：趋势较持续、噪音相对可控的中长线行情。
- 优点：简单、可解释、稳健、参数少。
- 缺点：震荡市来回打脸，信号滞后。

## 2) `donchian`（通道突破）
- 核心规则：突破 N 日最高做多，跌破 N 日最低做空；回到通道中线附近平仓。
- 假设：关键区间被突破后，趋势具有延续性。
- 适用场景：有明显突破-延续特征的市场。
- 优点：能较早抓住大趋势，规则清晰。
- 缺点：假突破较多；参数对交易频率影响明显。

## 3) `momentum`（动量 + 波动率过滤）
- 核心规则：中周期动量显著为正且波动率不过高时做多；显著为负时做空；其余空仓。
- 假设：中周期动量可持续，极端高波动阶段更易失效。
- 适用场景：中期趋势明确，且风险环境相对稳定。
- 优点：兼顾方向与风险过滤，减少无效交易。
- 缺点：阈值敏感，参数需按标的做稳健性验证。

## 4) `regime`（动量 + 趋势过滤 + 波动率过滤）
- 核心规则：中周期动量超过阈值且价格位于长期均线上方并满足波动率过滤时做多；反向条件做空；其余空仓。
- 假设：趋势与动量需同时确认，且高波动阶段更易失效。
- 适用场景：中长线趋势较清晰、但存在波动率阶段切换的市场。
- 优点：相比纯动量更稳健，过滤掉部分噪音区间。
- 缺点：过滤条件较严格，可能错过早期拐点。

## 5) `macd_regime`（MACD 趋势确认 + regime/波动率过滤）
- 核心规则：`MACD 线 > 信号线` 且 `MACD/价格` 超过阈值并满足趋势/波动率过滤时做多；反向条件做空；其余空仓。
- 假设：中长线趋势中，MACD 的方向确认可减少纯均线交叉的噪音；高波动与逆趋势阶段更容易失效。
- 适用场景：趋势延续性较强且存在阶段性波动聚集的市场。
- 优点：在趋势确认、regime、风险过滤之间取得平衡，解释字段更完整。
- 缺点：参数较多（EMA 周期、阈值、过滤窗口），需要 walk-forward 做稳健性验证。
- 参数建议区间（中长线）：
  - `fast_span`: `18 ~ 30`（默认对照模板 `24`）
  - `slow_span`: `48 ~ 80`（默认对照模板 `52`）
  - `signal_span`: `9 ~ 24`（默认对照模板 `18`）
  - `macd_ratio_threshold`: `0.0003 ~ 0.0012`（默认对照模板 `0.0005`）
  - `regime_window`: `120 ~ 220`（默认对照模板 `180`）
  - `volatility_window`: `15 ~ 30`（默认对照模板 `20`）

## 6) `score_regime`（多指标加权评分 + 波动率过滤）
- 核心规则：将中周期动量、长期趋势偏离、短周期均值回归三类得分按权重加总，得分高于阈值做多、低于负阈值做空，且需通过波动率过滤。
- 假设：单一指标在不同市场状态下会失真，多因子加权可提升中长线择时鲁棒性。
- 适用场景：趋势与震荡交替、需要在“跟随趋势”和“避免过度追涨杀跌”之间平衡的市场。
- 优点：可解释性强（可拆分到单因子得分），参数可按研究结果调权。
- 缺点：参数维度更高，过拟合风险上升，需要结合 walk-forward 和稳定性统计约束。
- 参数建议区间（中长线）：
  - `momentum_window`: `84 ~ 168`（默认对照模板 `126`）
  - `trend_window`: `150 ~ 260`（默认对照模板 `200`）
  - `mean_revert_window`: `10 ~ 30`（默认对照模板 `20`）
  - `score_threshold`: `0.18 ~ 0.35`（默认对照模板 `0.25`）
  - 权重建议：`momentum_weight + trend_weight + mean_revert_weight = 1.0`，推荐起点 `0.4/0.4/0.2`
  - `volatility_window`: `15 ~ 30`，`volatility_ceiling`: `0.015 ~ 0.04`（优先按标的波动特征调参）

## 7) `atr_regime`（ATR 归一化趋势强度 + 波动率分位过滤）
- 核心规则：计算 `trend_strength = (close - trend_ma) / ATR`，当强度高于阈值且 ATR 比率低于历史分位上限时做多；反向条件做空；其余空仓。
- 假设：趋势强度应按波动率归一化评估，极端波动阶段（ATR 比率过高）应减少参与。
- 适用场景：有中期趋势、但波动聚集阶段明显且需要规避“高波动追涨杀跌”的市场。
- 优点：将“方向强度”和“波动风险”合并到统一尺度，跨标的可比性更好。
- 缺点：ATR 与分位窗口均会引入滞后，拐点阶段可能反应偏慢。
- 参数建议区间（中长线）：
  - `atr_window`: `14 ~ 30`（默认 `20`）
  - `trend_window`: `120 ~ 220`（默认 `180`）
  - `strength_threshold`: `0.8 ~ 1.5`（默认 `1.0`）
  - `atr_cap_quantile`: `0.7 ~ 0.9`（默认 `0.8`）

## 8) `dual_momentum`（双周期动量共振 + 趋势/波动率过滤）
- 核心规则：短周期与长周期动量同时超过阈值，并且价格位于长期均线上方且波动率未超阈值时做多；反向条件做空；其余空仓。
- 假设：中长线趋势中的“多周期同向共振”比单周期动量更稳健，能降低噪音反转导致的误触发。
- 适用场景：中期与长期趋势相对一致、但阶段波动并不平滑的市场。
- 优点：相比单一动量策略更强调趋势一致性，信号更稳健，可解释字段清晰（快慢动量、regime、vol filter）。
- 缺点：过滤条件更严格，容易在趋势早期与震荡反弹阶段缺席。
- 参数建议区间（中长线）：
  - `fast_window`: `42 ~ 84`（默认 `63`）
  - `slow_window`: `189 ~ 252`（默认 `252`）
  - `regime_window`: `160 ~ 220`（默认 `200`）
  - `fast_threshold`: `0.015 ~ 0.03`（默认 `0.02`）
  - `slow_threshold`: `0.04 ~ 0.08`（默认 `0.06`）
  - `volatility_quantile`: `0.75 ~ 0.9`（默认 `0.8`）

## 统一约束
- 禁止未来函数：信号按前一根 bar 执行。
- 明确成本：回测/模拟均计入手续费与滑点。
- 支持方向：做多 / 做空 / 空仓。
