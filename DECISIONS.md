# DECISIONS

## D-195 simulate 恢复采用“幂等跳过重复 bar”
- 日期：2026-03-16
- 决策：`PaperBroker` 增加 `last_processed_at`（按 symbol 记录最后处理时间），`simulate` 重跑同一时间窗口时自动跳过已处理 bar。
- 原因：此前恢复依赖“重放历史 + 状态叠加”，在重复区间下会重复开平仓与重复记账，影响结果可信度与可维护性。
- 取舍：状态文件新增字段并引入时间标准化逻辑（UTC ISO），代码复杂度小幅上升，但恢复链路的确定性显著提高。

## D-001 架构分层
- 日期：2026-03-15
- 决策：采用 `data / strategies / backtest / simulation` 四层模块化结构。
- 原因：便于替换数据源、复用策略、隔离执行引擎。
- 取舍：初期文件数增加，但维护成本更低。

## D-002 MVP 优先级
- 日期：2026-03-15
- 决策：先完成命令行可运行 MVP，再做增强功能。
- 原因：任务明确要求先交付最小可用版本并保持持续可运行。
- 取舍：暂不优先做 Web 可视化。

## D-003 ETH 免费数据源选择
- 日期：2026-03-15
- 决策：使用 Yahoo `ETH-USD` 作为 ETH 主数据源。
- 原因：`Binance` 在当前运行环境出现 `451` 限制，影响稳定可用性。
- 取舍：符号层面以 `ETH`/`ETHUSD`/`ETHUSDT` 统一映射到 `ETH-USD`，优先保证可运行与可复现。

## D-004 执行与回测一致性
- 日期：2026-03-15
- 决策：回测采用“前一根信号、下一根执行”的时序约束，并统一计入滑点与手续费。
- 原因：避免未来函数，提升研究可信度。
- 取舍：收益曲线更保守，但更接近真实执行。

## D-005 回测接口收敛
- 日期：2026-03-15
- 决策：统一回测对外接口为 `BacktestEngine.run()`，结果模型固定为 `BacktestResult`。
- 原因：历史代码存在重复结构，容易引发调用方混用与维护成本上升。
- 取舍：需要一次性迁移示例/调用代码，但后续扩展更稳定。

## D-006 策略接口与注册收敛
- 日期：2026-03-15
- 决策：所有策略统一实现 `generate_signals()`，并通过注册表对外暴露固定名称。
- 原因：历史代码中 `generate_signal`/`generate_signals` 混用，容易导致运行时错误。
- 取舍：需要重构旧策略文件，但可显著降低接口歧义。

## D-007 模拟交易状态持久化增强
- 日期：2026-03-15
- 决策：`PaperBroker` 在状态文件中增加手续费、滑点、总权益与收益率相关字段。
- 原因：仅记录持仓与已实现收益不足以复盘执行质量。
- 取舍：状态文件字段增多，但可观测性和恢复一致性显著提升。

## D-008 参数研究流程
- 日期：2026-03-15
- 决策：新增 `research` 模块，先做“参数网格 + walk-forward”基础版，按窗口内训练最优参数并在下一测试窗口验证。
- 原因：MVP 可运行后，最优先补充可复现的稳健性评估能力，降低一次性样本过拟合风险。
- 取舍：当前优先单策略单标的研究，尚未覆盖组合级参数联动优化。

## D-009 策略参数注入方式
- 日期：2026-03-15
- 决策：CLI 使用 `--params` JSON 注入策略构造参数；`research` 使用 `--grid` JSON 传入参数网格。
- 原因：无需新增配置文件即可快速试验，且与脚本自动化集成简单。
- 取舍：命令行 JSON 可读性一般，后续可补 YAML 配置文件以提升可维护性。

## D-010 排行榜评分口径
- 日期：2026-03-15
- 决策：`compare` 使用 Sharpe/Calmar/总收益/最大回撤四指标加权评分（0.35/0.25/0.25/0.15）。
- 原因：单一指标容易偏科，综合评分更适合中长线策略初筛。
- 取舍：当前为静态权重，后续可按市场状态或风险偏好动态调权。

## D-011 配置驱动优先 JSON
- 日期：2026-03-15
- 决策：先实现 `run-config` + JSON 配置模板，不额外引入 YAML 依赖。
- 原因：减少依赖与解析复杂度，先打通自动化实验工作流。
- 取舍：可读性不如 YAML，后续若配置规模继续增长再引入 YAML 层。

## D-012 风控落点选择（回测级开关）
- 日期：2026-03-15
- 决策：在 `BacktestEngine` 增加可选 `max_drawdown + cooldown_bars` 风控开关，而非先改动策略接口。
- 原因：可以在不破坏现有策略实现的前提下，统一对所有策略施加一致风险约束。
- 取舍：当前风控是回测执行层逻辑，尚未下沉到组合级资金管理器；但落地快、风险低、兼容性好。

## D-013 信号解释放在策略层
- 日期：2026-03-15
- 决策：为策略接口新增 `explain()`，默认返回 `signal+reason`，并由回测/模拟统一落盘。
- 原因：解释信息与策略内部因子强相关，放在策略层最容易保证语义一致。
- 取舍：策略文件代码量略增，但调试和复盘效率显著提升。

## D-014 组合回测先做等权基线
- 日期：2026-03-15
- 决策：新增 `portfolio` 命令，组合层先使用“固定等权 + 各标的独立信号”的资金分配。
- 原因：这是最稳定、最易复现的组合基线，便于后续迭代到风险预算模型。
- 取舍：当前未实现动态风险平价与杠杆控制，但工程复杂度可控、可快速验证多标的联动效果。

## D-015 报告层先做离线汇总
- 日期：2026-03-15
- 决策：新增 `report` 命令，从 `outputs` 工件自动生成 `report_index.md`，暂不引入数据库或可视化服务。
- 原因：离线文件汇总足以满足当前复盘和协作需要，且不会增加部署复杂度。
- 取舍：可视化能力较弱，但结构简单、依赖少、稳定性高。

## D-016 组合资金分配路线
- 日期：2026-03-15
- 决策：在 `portfolio` 模块引入三档分配模式：`equal`（基线）、`risk_parity`（逆波动权重）、`vol_target`（风险平价基础上叠加目标波动率杠杆）。
- 原因：中长线多资产组合需要先解决“各资产风险不均衡”问题，再控制整体波动率；该路线能在较低复杂度下显著提升组合层可用性。
- 取舍：当前使用滚动历史波动率近似风险预算，未引入协方差矩阵与交易容量约束；后续可升级为全量风险模型。

## D-017 组合模拟账户执行方式
- 日期：2026-03-15
- 决策：新增 `simulate-portfolio` 命令，采用“多标的共同时间轴 + 单账户共享资金 + 每信号权益占比下单”模型。
- 原因：在不重构 `PaperBroker` 内核的情况下，最快实现多标的组合模拟与状态持久化，满足任务书要求的可恢复与收益跟踪。
- 取舍：当前资金约束与保证金规则简化，未实现真实券商级风控；后续可在账户层加入可用资金与杠杆限制。

## D-018 组合归因先采用线性近似
- 日期：2026-03-15
- 决策：组合归因输出采用“逐标的净贡献（权重*收益-换手成本）+ 年化波动贡献 + 总换手”线性框架。
- 原因：线性框架可直接复用回测结果、实现简单且可复现，适合作为当前 MVP 后阶段的可解释性基线。
- 取舍：未引入协方差分解与因子归因，风险贡献仍是近似口径；后续再升级到协方差矩阵级归因。

## D-019 参数稳定性口径
- 日期：2026-03-15
- 决策：新增 `stability` 命令，以 walk-forward 明细为输入，输出“参数值频次 + Sharpe 稳定性摘要”。
- 原因：比单次最优参数更能反映策略在不同时间窗口的一致性，且不增加回测主流程复杂度。
- 取舍：当前不绘图，仅导出 CSV/JSON；后续可叠加可视化与参数漂移告警。

## D-020 组合模拟风险限额落地位置
- 日期：2026-03-15
- 决策：在 `simulate-portfolio` 执行循环中直接加入风险限额（单标的仓位上限、组合总仓位上限、回撤触发强平与冷却期），并保持 `PaperBroker` 内核通用。
- 原因：风险限额是组合策略层约束，放在命令层可快速迭代规则且不破坏单标的模拟接口。
- 取舍：当前风控参数按命令注入，尚未抽象为独立账户风控引擎；但实现成本低、兼容性高。

## D-021 协方差预算模式扩展
- 日期：2026-03-15
- 决策：在 `portfolio` 分配模式中新增 `cov_risk_parity` 与 `cov_vol_target`，使用滚动协方差逆矩阵近似权重，再可选叠加目标波动率杠杆。
- 原因：原有 `risk_parity/vol_target` 仅基于单资产波动率，难以处理资产相关性变化；协方差近似能在低复杂度下提升风险预算质量。
- 取舍：当前为近似解（非严格风险预算求解），并依赖历史窗口稳定性；后续可迭代为完整约束优化器。

## D-022 组合告警输出口径
- 日期：2026-03-15
- 决策：在组合回测明细中新增 `alerts` 表，按滚动相关性与同向拥挤度输出 `watch/high` 两级告警，并写入 `portfolio_alerts_*.csv`。
- 原因：组合研究不仅要看收益与回撤，还需跟踪“相关性上升 + 同向拥挤”导致的潜在尾部风险。
- 取舍：当前阈值为固定常量（相关性 0.75/0.85、拥挤度 0.15/0.2），后续可按市场状态做配置化或自适应。

## D-023 风控测试优先覆盖关键路径
- 日期：2026-03-15
- 决策：新增组合模拟回归测试，优先验证“回撤触发强平 + 冷却期阻断新开仓”链路，而非一次性覆盖全部资金细节。
- 原因：该链路最容易引发实盘偏差，优先纳入自动化回归可显著降低后续迭代风险。
- 取舍：当前测试使用构造数据与假策略，尚未覆盖更多实盘边界（如跳空、连续反转、资金约束）。

## D-024 组合权重工件单独落盘
- 日期：2026-03-15
- 决策：在 `portfolio` 命令新增 `portfolio_weights_*.csv` 输出，保留每个时间点的权重序列，并在 `report` 汇总平均总暴露。
- 原因：协方差预算迭代后需要直接观测权重与杠杆变化，单看最终净值不利于排查漂移。
- 取舍：输出工件数量增加，但诊断效率明显提升，且对主流程性能影响很小。

## D-025 组合告警阈值参数化
- 日期：2026-03-15
- 决策：将组合告警阈值从固定常量改为可配置参数，新增 `corr_watch/high_threshold` 与 `crowding_watch/high_threshold`，并在 `portfolio` CLI 与 `run-config` 中暴露。
- 原因：不同市场状态下“相关性风险”与“拥挤度风险”阈值应可调，固定常量不利于批量实验复现与横向比较。
- 取舍：参数数量增加，命令行复杂度略有上升；通过默认值保持兼容，并在配置文件示例中提供模板。

## D-026 组合模拟回归测试优先覆盖资金边界与反转链路
- 日期：2026-03-15
- 决策：新增两类组合模拟回归场景：`max_total_allocation` 耗尽时阻断新开仓、连续多次反转时可连续完成平仓+反向开仓。
- 原因：这两类场景最容易在资金分配迭代中被破坏，且会直接影响模拟交易结果可信度。
- 取舍：当前仍采用简化账户模型（未引入保证金/借贷），先通过回归测试锁定现有行为，再迭代资金模型细节。

## D-027 组合偏移统计口径
- 日期：2026-03-15
- 决策：新增 `portfolio_drift_*.csv`，按“上一期目标权重在本期收益冲击后的漂移权重”与“本期目标权重”做对比，导出逐标的 `weight_gap` 与组合级 `abs_weight_gap_sum`。
- 原因：组合诊断需要区分“信号目标权重”与“价格变动导致的自然偏移”，仅观察权重快照不足以评估再平衡压力。
- 取舍：当前偏移使用线性近似且按绝对权重归一化，易复现但非交易所级精确成交模型；后续可叠加交易时延与成交约束。

## D-028 组合漂移告警阈值配置化
- 日期：2026-03-15
- 决策：在组合回测中新增 `drift_watch_threshold` 与 `drift_high_threshold`，按 `abs_weight_gap_sum` 生成 `drift_alert_level`（`none/watch/high`），并在报告层汇总告警计数。
- 原因：仅有平均/最大偏移不足以支撑批处理筛选，需要统一告警口径快速识别“再平衡压力过高”的组合阶段。
- 取舍：当前仍为固定阈值，不含自适应状态切换；通过 CLI/配置文件参数化先保证实验可复现与可对比。

## D-029 组合模拟资金占用模型（保证金 + 现金保留）
- 日期：2026-03-15
- 决策：在 `simulate-portfolio` 增加 `initial_margin_rate` 与 `cash_reserve_ratio`，开仓时同时受“总仓位上限 + 单标的上限 + 可用保证金 + 现金预留”共同约束。
- 原因：仅按仓位占比限制不能反映资金占用强度，容易在多标的同向信号下出现过度占用；引入保证金与现金预留后更接近实盘资金管理。
- 取舍：当前为近似保证金模型（不区分多空品种规则与维持保证金），优先保证可解释和稳定回归，后续再细化到品种级规则。

## D-030 漂移告警连续段统计
- 日期：2026-03-15
- 决策：在 `report` 汇总 `portfolio_drift_*.csv` 时，新增 `max_high_streak` 与 `high_segments` 统计，和 high/watch 次数一起输出。
- 原因：仅统计 high 告警总数无法区分“零散噪声”与“持续失衡”，连续段指标更适合识别需要强制再平衡的风险阶段。
- 取舍：当前按文件原始时间顺序计算连续段，未做跨文件聚合；先保证实现简单和结果直观，后续可升级到组合维度聚合。

## D-031 组合模拟资金快照工件
- 日期：2026-03-15
- 决策：`simulate-portfolio` 在信号日志之外新增 `sim_portfolio_capital_*.csv`，逐 bar 记录总权益、总暴露、保证金占用、可用保证金与预留现金。
- 原因：保证金约束生效后，需要可追踪的账户级时间序列来定位“为何被阻断开仓”，仅凭信号 reason 不足以复盘资金状态。
- 取舍：当前快照以命令层估算口径输出，尚未写入 broker 状态文件；先保证分析透明度，再评估是否下沉到通用账户层。

## D-032 `run-config` 批处理入口
- 日期：2026-03-15
- 决策：`run-config` 支持 `command: "batch"` 与 `tasks` 列表，按顺序串行执行多个任务；`tasks` 每项可为内联配置对象或相对路径文件引用。
- 原因：组合研究与报告常需要固定流水线（抓取/回测/汇总）重复执行，单文件单任务模式操作成本高。
- 取舍：当前为串行执行，不做并发调度与失败重试；优先保证行为确定性与复现实验顺序。

## D-033 报告层加入组合资金占用摘要
- 日期：2026-03-15
- 决策：`report` 在汇总中新增 `sim_portfolio_capital_*.csv` 解析，输出平均暴露率、平均/峰值保证金利用率、平均预留现金占比。
- 原因：组合模拟已输出资金快照，但若不做汇总，批量实验难以快速判断“资金约束是否过紧/过松”。
- 取舍：当前仅做文件级摘要，不做跨文件聚合排名；先满足复盘效率，再考虑全局对比报表。

## D-034 漂移告警联动再平衡实验口径
- 日期：2026-03-15
- 决策：在组合回测中新增可选“漂移告警触发额外再平衡成本”机制（`rebalance_on_drift`），当 `drift_alert_level` 达到触发阈值时注入额外换手与成本，并导出 `portfolio_rebalance_*.csv`。
- 原因：此前漂移告警仅用于诊断，无法量化“执行再平衡纪律”对收益与成本的影响；联动实验可形成可复现的策略取舍依据。
- 取舍：当前属于研究口径（成本注入），不模拟逐笔成交路径；优势是实现稳定、参数可控，后续可升级为更细粒度撮合模型。

## D-035 批处理失败策略与汇总
- 日期：2026-03-15
- 决策：`run-config` 批处理新增 `on_error`（`fail_fast`/`continue`）；`continue` 模式下不中断后续任务，最后统一抛出失败汇总。
- 原因：研究流水线中常需“一次跑完再看全局结果”，但也需要保留默认严格模式避免静默失败。
- 取舍：当前汇总以终端文本抛错为主，尚未落盘结构化结果；先保证可用性与行为可预期。

## D-036 再平衡实验报告排序口径
- 日期：2026-03-15
- 决策：`report` 在 `portfolio_rebalance_*.csv` 多文件场景下，按 `avg_cost_per_trigger` 给出 best/worst 两端对比。
- 原因：再平衡实验常成批运行，仅看单文件摘要不利于快速筛选“成本更低”的参数组合。
- 取舍：当前只输出两端摘要，不输出完整排名表；先降低阅读噪音，后续可按需扩展为全量排行榜。

## D-037 批处理摘要默认导出
- 日期：2026-03-15
- 决策：`run-config` 在批处理任务（task 数 > 1）且未显式配置摘要路径时，默认输出 `outputs/run_config_batch_summary.json` 与 `outputs/run_config_batch_summary.csv`。
- 原因：CI/定时批处理需要稳定工件用于追踪成功率与失败任务，默认开启比“可选但常被漏配”更稳妥。
- 取舍：输出工件数量增加，但定位失败链路明显更快；单任务配置仍保持不强制输出。

## D-038 批处理模板变量占位符
- 日期：2026-03-15
- 决策：`run-config` 新增顶层 `vars` 与 `${name}` 占位符替换，支持 `tasks[].vars` 对单任务局部覆盖。
- 原因：一体化流水线中 `start/end/symbols/tag` 等参数重复率高，模板变量可减少拷贝错误并提升可维护性。
- 取舍：解析逻辑更复杂；通过“缺失变量即报错”保证行为可预测，避免静默使用错误参数。

## D-039 `on_error=continue` 最终失败语义
- 日期：2026-03-15
- 决策：`run-config` 在 `on_error=continue` 下执行完全部任务后，若存在失败项则抛出 `RuntimeError` 汇总。
- 原因：研究流水线需要“尽可能跑完”，但 CI 也必须识别失败；末尾抛错可同时满足两者。
- 取舍：与“仅打印失败不报错”相比更严格，可能影响旧脚本；但可避免批处理失败被误判为成功。

## D-040 批处理摘要增加运行级耗时
- 日期：2026-03-15
- 决策：在 `run_config_batch_summary.json` 中新增 `run_started_at`、`run_ended_at`、`run_duration_ms` 三个运行级字段。
- 原因：仅有任务级耗时不便于追踪整次流水线成本，CI/定时任务更需要运行级指标做基线监控。
- 取舍：摘要结构略复杂，但兼容旧字段；CSV 仍保持任务明细为主，避免列膨胀。

## D-041 预置保守/激进批处理模板
- 日期：2026-03-15
- 决策：新增 `config.batch.portfolio_conservative.json` 与 `config.batch.portfolio_aggressive.json` 两套预设，基于同一占位符结构调整仓位/杠杆/风控参数。
- 原因：中长线研究常需要快速做风险偏好对比，直接提供两档可运行模板比手工改参更稳妥。
- 取舍：示例数量增加；通过统一 `vars` 与固定任务链路降低维护成本。

## D-042 批处理重试语义细化
- 日期：2026-03-15
- 决策：`run-config` 在全局 `retry_count/retry_delay_ms` 基础上，新增 `retry_enabled`、`retry_retryable_only`，并支持任务级 `retry` 覆盖。
- 原因：不同任务失败类型差异明显（配置错误 vs 网络抖动），需要默认“只重试可重试错误”以避免无效重试和噪音日志。
- 取舍：配置复杂度上升；通过默认值保持兼容（不开启或仅重试可重试异常）。

## D-043 批处理失败命令 TopN 报告
- 日期：2026-03-15
- 决策：`report` 在汇总 run-config 摘要时新增 `failed_top_commands`（失败次数最多的命令 TopN）。
- 原因：批处理规模变大后，单看失败明细不利于快速定位系统性薄弱环节。
- 取舍：当前只输出 TopN 文本，不输出完整透视表；先保证可读性，再按需要扩展。

## D-044 批处理聚合口径双视角（全量 + 最近窗口）
- 日期：2026-03-15
- 决策：`report` 的批处理摘要同时输出 `aggregate_all_runs`（全量）与 `aggregate_last_N_runs`（最近窗口）两条聚合统计。
- 原因：全量统计用于长期基线，最近窗口用于观察近期退化；两者缺一都会降低排障效率。
- 取舍：报告行数略增，但可显著降低“只看单次 run”造成的误判风险。

## D-042 批处理配置校验保持“严格失败”
- 日期：2026-03-15
- 决策：对 `tasks[].vars`、`tasks.file.vars`、`summary_json/summary_csv` 非法类型保持“解析阶段直接报错”，不做隐式容错转换。
- 原因：批处理配置常用于自动化任务，隐式兜底会放大误配置风险；尽早失败更利于定位问题。
- 取舍：配置灵活性略降，但可显著降低“带错运行”的隐患。

## D-043 报告层纳入批处理执行健康度
- 日期：2026-03-15
- 决策：`report` 新增“批处理执行摘要”段，识别 run-config 摘要工件并展示 `run_id`、成功/失败任务数、总耗时、最慢任务。
- 原因：批处理执行质量此前只在摘要文件中，缺少统一入口；纳入报告可让研究结果与执行健康度同屏复盘。
- 取舍：报告内容变长；通过仅提取关键指标（而非逐任务全量明细）控制阅读负担。

## D-044 预设模板必须配套复盘口径
- 日期：2026-03-15
- 决策：新增 `BATCH_PRESETS.md`，对保守/激进模板给出统一指标口径与解读建议。
- 原因：仅提供配置文件会导致复盘标准不一致，难以横向比较实验结论。
- 取舍：文档维护成本略增，但可显著降低团队/多会话协作时的口径漂移。

## D-045 批处理重试策略
- 日期：2026-03-15
- 决策：`run-config` 增加 `retry_count` 与 `retry_delay_ms`，任务失败时按配置重试；摘要写入 `attempts` 字段。
- 原因：数据拉取/外部接口类任务存在瞬时失败，缺少重试会降低批处理稳定性。
- 取舍：重试会增加总耗时；通过默认 `retry_count=0` 保持历史行为不变，由配置显式开启。

## D-046 批处理失败命令 TopN
- 日期：2026-03-15
- 决策：`report` 在批处理摘要段新增 `failed_top_commands` 聚合，按失败次数降序展示 TopN 命令。
- 原因：批处理失败常跨多个 run 分散出现，单文件查看成本高；聚合后更容易定位高风险命令。
- 取舍：仅给出计数，不展开失败详情；详细排查仍回到具体摘要文件。

## D-047 重试默认仅针对“可重试错误”
- 日期：2026-03-15
- 决策：默认启用 `retry_retryable_only=true`，并支持通过批处理级 `retry_*` 与任务级 `retry` 覆盖开关/次数/间隔。
- 原因：配置错误、未知命令等“确定性失败”盲目重试只会浪费时间并放大噪音。
- 取舍：默认更保守；当需要强制重试时可显式设置 `retry_retryable_only=false`。

## D-037 批处理执行摘要落盘
- 日期：2026-03-15
- 决策：`run-config` 批处理支持 `summary_json` / `summary_csv`，导出任务级成功/失败明细与计数统计。
- 原因：批处理在 CI 中运行时需要结构化产物用于追踪失败任务与回归差异，终端日志不够稳定。
- 取舍：当前摘要不含耗时与资源占用，仅先覆盖执行状态；后续再补时延指标。

## D-038 一体化批处理模板
- 日期：2026-03-15
- 决策：新增 `config.batch.portfolio_template.json`，统一 `fetch + portfolio + simulate-portfolio + report` 四段链路占位符模板。
- 原因：多次实验时重复复制配置容易漏改字段，模板化可降低操作失误并提升复现效率。
- 取舍：当前占位符为静态文本（`${...}`），不内建变量渲染器；由用户或外部脚本替换后执行。

## D-046 批处理重试默认改为“仅可重试错误”
- 日期：2026-03-15
- 决策：`run-config` 在启用重试时默认仅对可重试错误重试（网络/超时/限流与瞬时连接错误）；配置/参数/文件缺失等确定性错误不重试。
- 原因：确定性错误重复执行没有价值，会拉长流水线时延并掩盖根因。
- 取舍：默认行为更保守，若业务需要可通过 `retry_retryable_only=false` 显式切换为“失败即重试”。

## D-047 重试策略增加全局与任务级双层开关
- 日期：2026-03-15
- 决策：新增全局开关 `retry_enabled`、`retry_retryable_only`，并支持 `tasks[].retry` / `tasks.file.retry` 覆盖 `enabled/count/delay_ms/retryable_only`。
- 原因：批处理中不同任务稳定性差异大（例如数据抓取 vs 本地报告生成），需要细粒度控制重试成本。
- 取舍：配置复杂度上升；通过严格类型校验与摘要落盘（含 `retry_*` 字段）降低维护风险。

## D-048 报告层增加批处理跨文件聚合统计
- 日期：2026-03-15
- 决策：`report` 在批处理摘要段新增近 N 次运行聚合（默认近 10 次）：运行失败率、任务失败率、平均耗时、P50/P90 耗时。
- 原因：单次 run 只能看局部健康度，缺少跨运行趋势无法判断流水线稳定性是否在恶化。
- 取舍：统计口径更复杂；通过固定字段与回归测试锁定输出格式，避免后续迭代引入口径漂移。

## D-049 批处理总重试预算保护
- 日期：2026-03-15
- 决策：`run-config` 新增 `max_total_retries`（跨任务共享总重试预算），预算耗尽后不再继续重试；摘要落盘 `retry_budget_remaining` 与任务级 `retries_used`。
- 原因：仅按任务级 `retry_count` 限制会在多任务连续失败时造成尾部耗时失控，需要全局闸门保护批处理时延。
- 取舍：可能导致后续任务在预算耗尽后“快速失败”；但换来更稳定的总耗时上界与更可控的自动化行为。

## D-050 保守/激进模板一键对比落地为独立脚本
- 日期：2026-03-15
- 决策：新增 `scripts/compare_batch_presets.py`（底层复用 `research.preset_compare.compare_presets`），顺序执行两套预设并输出统一对比表 `outputs/preset_compare/preset_compare.csv/.md`。
- 原因：手工逐条运行并比对工件容易遗漏字段且不可重复，一键脚本能稳定固化复盘流程。
- 取舍：当前默认重跑两套批处理，耗时较长；通过 `--no-run` 提供离线工件对比模式平衡速度与准确性。

## D-051 报告层新增重试效率聚合指标
- 日期：2026-03-15
- 决策：在批处理聚合段新增 `retry_hit_rate`、`total_retries_used`、`non_retryable_fail_share` 三项指标。
- 原因：仅看失败率无法区分“重试带来恢复”与“重试无效消耗”，需要效率视角辅助调参。
- 取舍：口径依赖任务明细字段完整性；对缺字段场景采用保守回退（由 `attempts` 估算 `retries_used`）。

## D-053 预设对比支持命令行日期覆盖
- 日期：2026-03-15
- 决策：`scripts/compare_batch_presets.py` 新增 `--start/--end` 参数，通过运行时 `vars_override` 覆盖两套预设的 `vars.start/end`，不修改原配置文件。
- 原因：频繁比较不同时间区间时，手工改 JSON 模板容易出错且影响版本管理；命令行覆盖更稳妥、可复现。
- 取舍：运行时会生成临时配置文件并调用 `run-config`，流程略复杂；但避免污染示例配置并保持默认行为兼容。

## D-052 重试预算轨迹独立工件
- 日期：2026-03-15
- 决策：`run-config` 新增 `retry_budget_trace_csv`，按任务落盘预算轨迹（`budget_before/budget_after/retries_used/status`）；若批处理配置了 `max_total_retries` 且未显式指定路径，默认输出 `outputs/run_config_retry_budget_trace.csv`。
- 原因：摘要中的剩余预算是终态快照，无法解释预算在各任务间如何消耗；轨迹工件可直接支持跨 run 对比与排障。
- 取舍：输出工件数量增加，但字段稳定且体量小，对执行性能影响可忽略。

## D-054 ETH 双源接入顺序（Binance 主源 + Yahoo 回退）
- 日期：2026-03-15
- 决策：`ETH/ETHUSD/ETHUSDT` 统一优先走 `BinanceSpotProvider(ETHUSDT)`，失败后自动回退 `YahooFinanceProvider(ETH-USD)`。
- 原因：Binance 更贴近交易对语义，但在部分网络环境会遇到 `451`；双源顺序可同时保证“数据优先级”与“可运行性”。
- 取舍：数据口径可能在主源与回退源之间有细微差异；当前优先保证链路稳定，后续再补统一校准层。

## D-055 报告层新增重试预算轨迹摘要
- 日期：2026-03-15
- 决策：`report` 在检测到 `*retry_budget_trace*.csv` 时输出文件级与 aggregate 摘要（`depleted_events/avg_consumed/final_remaining`）。
- 原因：仅有批处理终态预算不足以解释重试预算如何被任务消耗，轨迹摘要可直接暴露预算耗尽风险。
- 取舍：当前摘要聚焦预算数量口径，尚未关联失败类型与命令分布；先保证可观测性，再做二阶分析。

## D-056 预设对比保留历史快照
- 日期：2026-03-15
- 决策：`compare_presets` 在写入 `preset_compare.csv/.md` 的同时，额外落盘 `preset_compare_<timestamp>.csv`，`report` 基于该快照输出 latest/prev 的 `total_return_gap` 变化。
- 原因：单次对比无法观察参数/市场窗口变化下的趋势，历史快照可支撑连续复盘与回归观察。
- 取舍：工件数量增加；通过仅写 CSV 快照（不重复写 MD）控制存储和噪音。

## D-057 报告层纳入预设对比摘要
- 日期：2026-03-15
- 决策：`report` 自动读取 `outputs/preset_compare/preset_compare.csv`，新增“预设对比摘要”段，并输出激进-保守关键指标差值。
- 原因：一键对比结果此前只存在独立工件，未进入统一报告入口；纳入索引后可与批处理健康度和组合指标同屏复盘。
- 取舍：报告内容变长；先输出核心差值字段，避免完整表格造成噪音。

## D-058 报告层纳入重试预算轨迹摘要
- 日期：2026-03-15
- 决策：`report` 读取 `*retry_budget_trace*.csv` 输出“重试预算轨迹”段，按文件汇总 `depleted_events`、`avg_consumed`、`final_remaining`，并追加 aggregate 行。
- 原因：预算轨迹虽已落盘，但缺少统一摘要入口；纳入报告可快速识别“预算是否频繁耗尽”与“平均消耗速度”。
- 取舍：当前只做文件级与简单 aggregate 统计，不引入更复杂的失败类型分层，先保证口径稳定可复现。

## D-059 预算轨迹摘要补充失败分布
- 日期：2026-03-15
- 决策：`report` 在“重试预算轨迹”段新增 `failed_rows`、`failed_ratio`、`depleted_failed`，并在 aggregate 行输出同口径汇总。
- 原因：仅看预算消耗无法判断是否与失败风险耦合，需要失败分布视角辅助定位“预算耗尽是否集中出现在失败任务”。
- 取舍：统计字段变多；先维持轻量文本摘要，不展开到命令级细分。

## D-060 预算轨迹摘要补充耗尽命令分布
- 日期：2026-03-15
- 决策：`report` 在预算轨迹摘要新增 `depleted_top_commands` 与 `aggregate_depleted_top_commands`，按预算耗尽任务中的命令频次输出 TopN。
- 原因：预算耗尽与失败率相关，但定位根因仍需要命令维度视角；TopN 可快速识别最易触发预算耗尽的任务类别。
- 取舍：当前只按频次输出文本 TopN，不展开完整透视表，优先保证阅读效率与落地成本。

## D-061 Binance 集成测试默认关闭
- 日期：2026-03-15
- 决策：新增 `BinanceSpotProvider` 真实网络集成测试，但默认跳过，需显式设置 `RUN_BINANCE_INTEGRATION=1` 才执行。
- 原因：外部网络与地域限制（如 `HTTP 451`）不稳定，默认关闭可避免 CI/本地回归随机失败；显式开启可用于周期性联通性验证。
- 取舍：默认测试覆盖仍以单元测试为主，无法保证每次回归都验证到真实网络链路；通过可选开关保留集成验证能力。

## D-062 重试预算轨迹按 run 聚合趋势
- 日期：2026-03-15
- 决策：`report` 在“重试预算轨迹”段新增 `aggregate_last_N_runs`，按最近 run 汇总 `depleted_run_rate`、`avg_depleted_rate`、`avg_failed_ratio`。
- 原因：文件级/全量 aggregate 无法回答“近期是否在恶化”，需要最近窗口趋势口径支撑运维与参数调优。
- 取舍：当前窗口固定为最近 10 次（不足则按实际数量），口径简洁但未细分到命令/策略维度；先保证稳定可复现。

## D-063 预设对比快照采用固定保留窗口
- 日期：2026-03-15
- 决策：`compare_presets` 新增 `keep_snapshot_count`（脚本参数 `--keep-snapshots`），默认仅保留最近 20 份 `preset_compare_<timestamp>.csv`；设置 `0` 表示不清理。
- 原因：长期运行会持续积累快照，若不清理会放大工件体积并拖慢目录扫描；固定窗口可控且符合近期复盘需求。
- 取舍：超出窗口的旧快照会被自动删除，不再保留全量历史；如需长期归档可显式设置 `--keep-snapshots 0` 或外部备份。

## D-064 脚本入口自举 `src` 路径
- 日期：2026-03-15
- 决策：`scripts/compare_batch_presets.py` 在启动时自动将项目 `src` 加入 `sys.path`，不再强依赖外部设置 `PYTHONPATH=src`。
- 原因：直接运行脚本是常见主链路，若依赖外部环境变量会触发 `ModuleNotFoundError` 并导致链路中断。
- 取舍：脚本增加少量引导代码，但换来开箱即用与更稳定的本地/CI 执行体验。

## D-065 批处理摘要支持 Gzip 压缩
- 日期：2026-03-15
- 决策：`run-config` 新增 `summary_compress`（`none/gzip`），在 `gzip` 模式下将 `summary_json/summary_csv` 输出为 `.gz` 工件；`report` 同步支持读取 `*summary*.json.gz`。
- 原因：批处理历史工件持续增长，摘要文件是高频产物；压缩可直接降低存储体积并保留完整字段。
- 取舍：读取链路增加“压缩/非压缩”分支；通过在报告层统一兼容 `.json/.json.gz` 避免用户手工解压。

## D-066 预设快照清理支持日期窗口
- 日期：2026-03-15
- 决策：`compare_batch_presets.py` 新增 `--snapshot-keep-start/--snapshot-keep-end`，`preset_compare` 在保留数量窗口之外，支持按快照时间戳区间清理历史 `preset_compare_<timestamp>.csv`。
- 原因：仅按数量清理无法满足“回看某一时段实验”与“删除异常时段快照”的运维需求，日期窗口能更精细地控制保留范围。
- 取舍：清理逻辑变复杂；为避免误删刚生成快照，当前实现会保护本次新生成文件不被区间清理移除。

## D-067 重试预算轨迹压缩输出
- 日期：2026-03-15
- 决策：`run-config` 新增 `retry_budget_trace_compress`（`none/gzip`），支持将 `retry_budget_trace_csv` 输出为 `.csv.gz`；`report` 同步兼容读取压缩轨迹文件。
- 原因：预算轨迹在批处理频繁运行时增长明显，压缩后能降低存储与归档成本，同时保持字段完整用于趋势分析。
- 取舍：配置项增多；通过默认 `none` 保持兼容，并沿用统一后缀补齐规则降低误用风险。

## D-065 预设对比趋势口径扩展到最近 N 次
- 日期：2026-03-15
- 决策：`report` 在预设对比段新增 `history.aggregate_last_N_snapshots`，输出 `avg_gap/min_gap/max_gap/improving_ratio`（默认最近 10 份快照）。
- 原因：仅看 latest/prev 容易受单次噪声影响，最近窗口聚合更适合判断激进-保守收益差是否持续改善。
- 取舍：摘要行更长，但仍保持轻量文本输出，不引入额外图表和工件。

## D-067 模块入口桥接包
- 日期：2026-03-15
- 决策：新增仓库根目录 `market_signal_system/__init__.py` 与 `market_signal_system/__main__.py`，把 `python3 -m market_signal_system` 桥接到 `src/market_signal_system/cli.py`。
- 原因：主链路中存在无 `PYTHONPATH` 场景，之前会直接报 `No module named market_signal_system`。
- 取舍：增加一层轻量入口代码，但换来更稳定的零配置启动能力。

## D-068 预设快照清理效果落盘
- 日期：2026-03-15
- 决策：`compare_presets` 在每次生成对比后额外输出 `preset_compare_cleanup_latest.json`，记录 `before/after/removed_by_date_range/removed_by_count/keep_snapshot_count`；`report` 同步在“预设对比摘要”输出 `cleanup.latest`。
- 原因：只有“当前快照列表”无法解释清理动作是否按预期执行，落盘清理统计能直接支撑运维排障与保留策略调优。
- 取舍：新增一个轻量 JSON 工件；通过只保留 latest 统计控制工件体积，不追踪全历史。

## D-069 预设快照维护模式
- 日期：2026-03-15
- 决策：`compare_batch_presets.py` 新增 `--cleanup-only`，调用 `compare_presets(cleanup_only=True)` 时仅执行快照清理与诊断落盘，不生成新的对比表。
- 原因：日常维护需要独立清理快照目录，若强制生成对比表会引入不必要计算与工件噪声。
- 取舍：`cleanup-only` 返回的 `preset_compare.csv/.md` 可能不存在；通过文档明确“该模式只做维护，不保证产出对比表”。

## D-070 压缩工件命名策略配置
- 日期：2026-03-15
- 决策：`run-config` 新增 `compress_naming`，支持 `auto_suffix`（默认自动补 `.gz`）与 `strict`（要求压缩路径显式带 `.gz`）两种模式，统一作用于摘要与预算轨迹压缩输出。
- 原因：部分批处理模板会混用“带后缀/不带后缀”路径，导致工件命名不一致；显式策略可降低误配与排障成本。
- 取舍：配置项增多；默认保持兼容，严格模式由需要强约束的流水线按需开启。

## D-071 清理摘要展示最近执行时间
- 日期：2026-03-15
- 决策：`report` 在 `cleanup.latest` 摘要中新增 `generated_at` 字段展示，作为“最近一次快照清理执行时间”提示。
- 原因：仅有 before/after 计数无法判断该清理结果是否新鲜，容易误读过期快照维护状态。
- 取舍：摘要行更长；先保持单行文本口径，不额外引入时间序列工件。

## D-072 严格压缩命名提供现成模板
- 日期：2026-03-15
- 决策：新增 `examples/config.batch.daily.strict_gzip.json`，默认启用 `summary_compress=gzip + compress_naming=strict` 并显式使用 `.gz` 输出路径。
- 原因：严格模式能防止混合后缀命名，但手工配置容易漏后缀；提供模板可直接复用并降低误配率。
- 取舍：示例文件数量增加；通过复用既有 daily 任务链路降低维护成本。

## D-073 严格命名覆盖预算轨迹压缩
- 日期：2026-03-15
- 决策：新增 `examples/config.batch.daily.strict_gzip_trace.json`，并补充 strict 模式下 `retry_budget_trace_compress=gzip` 的成功/失败回归测试。
- 原因：strict 策略若只覆盖摘要工件，预算轨迹仍可能出现后缀误配；补齐该路径可避免“部分文件严格、部分文件宽松”。
- 取舍：测试数量增加，但能显著降低压缩命名策略在批处理链路中的回归风险。

## D-072 提供 strict 压缩命名模板
- 日期：2026-03-15
- 决策：新增 `examples/config.batch.strict_compress.json`，作为 `compress_naming=strict` + `summary/retry_trace gzip` 的最小可运行示例。
- 原因：严格命名策略虽然能减少命名歧义，但手写配置更容易漏写 `.gz` 后缀；官方模板可降低首次使用门槛。
- 取舍：示例文件数量增加；通过最小任务链（单 `report`）控制维护成本。

## D-073 清理诊断增加历史序列与趋势聚合
- 日期：2026-03-15
- 决策：`preset_compare` 在写入 `preset_compare_cleanup_latest.json` 的同时，额外落盘 `preset_compare_cleanup_<timestamp>.json`；`report` 新增 `cleanup.aggregate_last_N_runs` 聚合（首末执行时间、时间跨度、平均/最大清理量）。
- 原因：仅看 latest 无法判断清理节奏是否稳定，历史序列可支撑“近期是否频繁清理/清理量是否异常”的趋势判断。
- 取舍：新增历史诊断工件会增加文件数量；先通过轻量 JSON 记录保障可观测性，后续再补保留策略。

## D-074 提供 strict 失败链路演示入口
- 日期：2026-03-15
- 决策：新增 `examples/config.batch.strict_failure_demo.json` 与 `scripts/demo_strict_failure_report.py`，用于一键复现“strict 压缩命名 + 失败任务 + 报告汇总”链路。
- 原因：strict 模式下失败场景是常见验收点，仅靠配置文件阅读不够直观；脚本入口可降低复盘成本并统一演示步骤。
- 取舍：增加一个专用演示脚本与示例配置；通过最小任务链（report/unknown/report）控制运行时长与维护开销。

## D-075 清理诊断历史保留窗口
- 日期：2026-03-15
- 决策：`compare_presets` 新增 `keep_cleanup_history_count`（脚本参数 `--keep-cleanup-history`），默认仅保留最近 50 份 `preset_compare_cleanup_<timestamp>.json`。
- 原因：补齐清理诊断历史后，若不做保留会导致诊断文件持续膨胀，影响目录扫描和长期维护。
- 取舍：会删除超窗口的旧诊断；如需长期归档可设为 `0`（不清理）并外部备份。

## D-076 strict 失败链路复盘口径前置到预设文档
- 日期：2026-03-15
- 决策：在 `BATCH_PRESETS.md` 增加 strict 失败演示专节，明确运行命令、预期失败语义与工件级 checklist。
- 原因：仅提供脚本入口仍需要阅读源码才能理解“失败是否符合预期”；把验收步骤前置到文档可降低夜间无人值守时的排障成本。
- 取舍：文档长度增加；但换来更稳定的一线复盘流程与更低的操作歧义。

## D-077 cleanup 历史聚合采用宽容解析策略
- 日期：2026-03-15
- 决策：`report` 在汇总 `preset_compare_cleanup_*.json` 时，对 `before/after/removed_*` 等字段采用数值安全转换，非法值降级为 `0`，非法时间记录直接跳过。
- 原因：清理诊断是运维辅助工件，脏数据不应阻断整份报告生成；应优先保证报告可用，再在摘要中保留可解释结果。
- 取舍：异常输入会被宽容处理而非直接报错，可能掩盖部分上游写入问题；后续可通过增加“异常记录计数”指标补足可观测性。

## D-078 cleanup 趋势摘要增加异常计数
- 日期：2026-03-15
- 决策：`report` 的 `cleanup.aggregate_last_N_runs` 新增 `skipped_invalid_time` 与 `rows_with_invalid_numeric` 两项统计。
- 原因：宽容解析避免了报告中断，但如果不暴露异常数量会削弱排障效率；补充计数可同时兼顾稳定性与可观测性。
- 取舍：摘要行更长，阅读噪音略增；但换来对脏数据规模的直接感知，适合批处理运维场景。

## D-079 strict 失败演示必须校验工件有效性
- 日期：2026-03-15
- 决策：为 `demo_strict_failure_report.py` 增加内容级回归断言，除退出码外同时验证 `summary/report` 工件存在且报告包含“批处理执行摘要”段。
- 原因：仅断言脚本成功执行不足以保证演示链路可复盘，必须覆盖“关键工件真实可用”这一主目标。
- 取舍：测试会触发真实工件写入，执行时长略增；但可显著降低“脚本成功但无有效产出”的漏检风险。

## D-080 README 与报告字段保持同口径演进
- 日期：2026-03-15
- 决策：将 `cleanup.aggregate_last_N_runs` 的新增异常计数字段（`skipped_invalid_time`、`rows_with_invalid_numeric`）同步写入 README 报告说明。
- 原因：该项目以 CLI + 文档作为主要协作界面，字段变更若不同步文档会增加误判与排障成本。
- 取舍：文档维护频率提升；但可降低后续多会话协作中的口径漂移。

## D-081 cleanup 聚合补充异常率与样本规模
- 日期：2026-03-15
- 决策：`cleanup.aggregate_last_N_runs` 新增 `history_files`、`valid_runs`、`abnormal_ratio`，用于表达样本覆盖与异常密度。
- 原因：仅有异常计数无法判断严重程度；增加分母与比率后，批处理巡检可快速识别“低频偶发”与“系统性脏数据”。
- 取舍：报告摘要更长；但信息密度更高，利于无人值守场景下快速判读健康度。

## D-082 首批策略扩展为 MACD 中长线变体
- 日期：2026-03-15
- 决策：新增 `macd_regime` 策略，采用 `MACD 线/信号线` 方向确认 + `MACD/价格` 阈值过滤，并叠加 `regime + 波动率` 风险开关。
- 原因：在现有均线/突破/动量之外，补充一种更偏“趋势确认 + 风险过滤”的中长线信号，增强策略簇多样性且保持可解释性。
- 取舍：参数数量增加（EMA/阈值/过滤窗口），需要依赖 walk-forward 做稳健性验证；但解释字段更完整，便于复盘与调参。

## D-083 模拟交易恢复能力以 CLI 链路回归锁定
- 日期：2026-03-15
- 决策：新增 `cmd_simulate` 连续两段数据运行的回归测试，验证“第一次运行留仓 -> 第二次运行加载历史并平仓”的状态延续行为。
- 原因：仅有 `PaperBroker` 单元测试不足以覆盖 CLI 组装链路（数据拉取/策略解释/状态落盘）的一致性风险。
- 取舍：测试夹具更复杂（需要 mock DataManager/Strategy 与临时目录），但能显著降低“模块单测通过、端到端恢复失败”的回归概率。

## D-084 新策略发布同步配置模板优先于口头约定
- 日期：2026-03-15
- 决策：新增 `examples/config.research.macd_regime.json`，并将 `examples/config.compare.json` 默认策略列表同步纳入 `macd_regime`。
- 原因：策略只接入代码而不进入示例模板，会导致后续批处理与回归入口仍停留旧策略集，形成“实现已存在但流程不可见”的维护断层。
- 取舍：示例配置数量继续增长；但对外可运行入口与内部能力保持一致，降低多会话协作误差。

## D-085 单策略复现入口优先落地为 backtest 模板
- 日期：2026-03-15
- 决策：新增 `examples/config.backtest.macd_regime.json`，作为 `macd_regime` 的最小回测复现模板，并将命令加入 README 的 `run-config` 示例清单。
- 原因：`research` 模板偏参数搜索，无法替代“固定参数快速复现”链路；补齐 `backtest` 模板后，单策略复盘成本更低。
- 取舍：示例文件增多；但换来研究模板与回测模板分工明确，降低误用研究配置做日常复盘的风险。

## D-086 CLI 恢复能力优先锁定子进程入口
- 日期：2026-03-15
- 决策：新增 `test_simulate_cli_can_resume_state_via_subprocess_without_network`，通过本地缓存数据驱动两段 `simulate` 子进程，验证同一 `state_file` 在重启后可继续累计交易状态。
- 原因：现有恢复回归偏函数级，仍存在“模块内可恢复、命令行入口失效”的潜在风险；子进程测试能直接覆盖真实启动路径。
- 取舍：测试会触发真实文件写入（cache/state），需在用例中显式清理；但可换来更强的端到端稳定性保证，且不依赖外网。

## D-087 回测工件命名采用可选输出标签
- 日期：2026-03-15
- 决策：为 `backtest` 增加 `--output-tag`，并在 `run-config` 的 `backtest` 默认参数中纳入 `output_tag` 字段，输出文件命名改为 `metrics/equity/trades/signals_<symbol>_<strategy>_<tag>`（tag 可选）。
- 原因：同策略多参数批处理时，原有固定文件名会被后续任务覆盖，导致参数敏感性实验无法保留并行工件。
- 取舍：输出文件命名规则更复杂；通过“标签可选、默认兼容旧路径”保持现有单次运行行为不变。

## D-088 参数敏感性模板优先采用 batch + report 串行
- 日期：2026-03-15
- 决策：新增 `examples/config.batch.macd_sensitivity.json`，在单次 `run-config` 中顺序执行三组 `macd_regime` 回测并自动生成 `macd_sensitivity_report.md`。
- 原因：参数敏感性分析需要稳定重复流程，模板化的 `batch` 配置比手工多次命令更易复现、更适合定时执行。
- 取舍：模板文件数量增加；但能显著降低实验操作成本，并将参数对照过程标准化。

## D-089 模拟恢复流程优先模板化为双阶段 simulate 批处理
- 日期：2026-03-15
- 决策：新增 `examples/config.batch.simulate_resume.json`，通过两段时间窗口 `simulate` 任务共享同一 `state_file`，并在末尾串接 `report`。
- 原因：恢复能力虽已有测试，但日常复盘仍依赖手工执行两次命令；模板化后可以一条命令稳定复现“断点续跑”链路。
- 取舍：模板进一步增加；但参数集中在 `vars`，维护成本可控，且更适合无人值守批处理场景。

## D-090 output_tag 命名标准采用“标的+策略+关键参数”
- 日期：2026-03-15
- 决策：新增 `examples/config.batch.output_tag_compare.json`，并将 `output_tag` 推荐命名规范明确为 `symbol + strategy + 关键参数`（示例：`qqq_macd_fast12_slow26_regime150`）。
- 原因：同策略多参数批处理若仅用短标签（如 `v1/v2`）会降低可读性，后续报告与工件巡检难以快速映射到参数集。
- 取舍：文件名更长；但定位成本更低，且与报告摘要/人工排障流程更一致。

## D-091 simulate 恢复验证优先提供离线脚本入口
- 日期：2026-03-15
- 决策：新增 `scripts/smoke_simulate_resume.py`，通过“本地缓存构造 + 两段 `simulate` 子进程 + 状态文件交易数递增断言”实现一键冒烟。
- 原因：恢复能力虽有测试，但运维/交接时仍需要可直接执行的人工验收入口；离线脚本可避免外网依赖并快速定位恢复链路是否可用。
- 取舍：脚本维护成本增加；但可显著降低“测试通过但现场不会复现”的执行落差。

## D-092 示例模板必须有解析级回归保护
- 日期：2026-03-15
- 决策：为 `examples/config.batch.output_tag_compare.json` 增加专用配置解析回归，断言前三个任务均为 `backtest + output_tag`，最后一个任务为 `report`。
- 原因：示例模板是自动化执行入口，若结构被误改（漏 `output_tag` 或任务顺序漂移）会直接破坏参数对照流程。
- 取舍：测试数量增加；但可稳定锁定模板契约，降低文档示例“看似可用、实际失效”的风险。

## D-093 离线恢复冒烟脚本强制日期顺序校验
- 日期：2026-03-15
- 决策：`smoke_simulate_resume.py` 在执行前强制校验 `start < split-end < end`，并对非法日期格式使用 `parser.error` 直接中断。
- 原因：恢复冒烟依赖两段时间窗口，若时间顺序错误会产生误导性失败；前置校验可将错误定位为“参数问题”而非“交易引擎问题”。
- 取舍：脚本入口增加约束，灵活性略降；但能显著减少无效排障时间。

## D-094 多指标评分策略优先作为中长线增强入口
- 日期：2026-03-15
- 决策：新增 `score_regime` 策略，采用“动量 + 趋势偏离 + 均值回归”三因子加权评分，并叠加波动率过滤；当综合得分越过正/负阈值时分别做多/做空，否则空仓。
- 原因：现有策略以单一逻辑为主（均线、突破、动量、MACD），在状态切换市场中容易偏科；引入可解释的加权评分可提高策略簇多样性并便于参数研究。
- 取舍：参数维度上升（窗口、权重、阈值），过拟合风险更高；通过默认权重归一化、契约测试与后续 walk-forward 研究控制风险。

## D-095 新策略发布必须同步研究模板与离线 CLI 回归
- 日期：2026-03-15
- 决策：为 `score_regime` 同步新增 `examples/config.research.score_regime.json`，并增加 `compare` 子进程离线回归，覆盖默认策略集（含新策略）在无 `PYTHONPATH` 场景下可执行。
- 原因：仅注册策略不足以保证工程可用，缺少模板会导致研究入口不可见，缺少 CLI 回归会导致默认命令在后续迭代中悄然失效。
- 取舍：模板与测试数量继续增长；但能显著降低“代码已实现、流程不可运行”的回归风险。

## D-096 新策略必须提供“研究模板 + 单策略复现模板”双入口
- 日期：2026-03-15
- 决策：在已有 `config.research.score_regime.json` 基础上补充 `examples/config.backtest.score_regime.json`，并以回归测试锁定模板解析契约。
- 原因：`research` 偏参数搜索，不适合快速复盘；`backtest` 固定参数模板可作为最小复现入口，便于日常巡检与批处理串联。
- 取舍：示例文件继续增加；但可显著降低“只有研究入口、缺少稳定复现入口”的运维风险。

## D-097 稳定性分析模板优先采用 research->stability->report 串联
- 日期：2026-03-15
- 决策：新增 `examples/config.batch.score_regime_stability.json`，在一个 `run-config` 批处理中顺序执行 `research`、`stability`、`report`。
- 原因：`score_regime` 参数维度较高，若没有串联模板，参数搜索与稳定性复盘容易分离，导致研究结果难以沉淀为可复现流程。
- 取舍：批处理模板进一步增多；但换来稳定的一键复盘入口，更适合无人值守执行与多会话交接。

## D-098 先补“数据质量诊断入口”再继续扩策略
- 日期：2026-03-15
- 决策：新增 `diagnose-data` 命令与 `diagnose_ohlcv_quality` 模块，优先覆盖缺失值、异常价格、OHLC 边界一致性、时间跳点与异常行导出。
- 原因：当前数据层已具备抓取/缓存/增量，但缺少“质量可观测”入口；先补诊断能力可以在后续策略研究与模拟交易前快速排查数据异常，避免把数据问题误判为策略问题。
- 取舍：增加了一条 CLI 与测试维护成本；但换来统一、可脚本化的数据体检流程，降低长期排障成本。

## D-099 单标的 simulate 引入权益比例仓位作为默认增强路径
- 日期：2026-03-15
- 决策：为 `simulate` 增加 `--allocation-per-signal` 与 `--min-quantity`，当启用比例仓位时按当前账户 `total_equity` 动态计算开仓数量；数量低于阈值则阻断开仓并记录原因。
- 原因：固定 `quantity` 对不同资产价格与账户规模不具可比性，难以做中长线稳健实验；权益比例仓位更接近真实风控流程。
- 取舍：参数面增加，日志字段更复杂；但默认保持兼容（未设置比例时仍走固定数量），对既有链路无破坏。

## D-100 simulate 资金曲线默认落盘
- 日期：2026-03-15
- 决策：`simulate` 每根 bar 追加资金快照并默认导出 `sim_equity_<symbol>_<strategy>.csv`，字段包括 `cash/realized_pnl/floating_pnl/cumulative_pnl/total_equity/return_pct`。
- 原因：此前仅输出最终快照与信号/成交日志，无法复盘中间风险路径；资金曲线是中长线策略验收的基础工件。
- 取舍：输出文件增多、磁盘占用略升；但换来可视化与风险排障能力，收益远高于成本。

## D-101 报告索引纳入单标的 simulate 资金曲线摘要
- 日期：2026-03-15
- 决策：`report` 新增“单标的模拟资金曲线摘要”段，自动统计 `sim_equity_*.csv` 的 bar 数、起止权益、总收益与最大回撤。
- 原因：`simulate` 资金曲线已落盘，如果报告层不聚合，批处理巡检仍需手工逐文件查看。
- 取舍：报告长度增长；但关键模拟结果可一屏查看，更适合无人值守日报与回顾。

## D-102 策略专题对照优先落地为批处理模板
- 日期：2026-03-15
- 决策：新增 `examples/config.batch.score_vs_macd_compare.json`，在同一时间窗内顺序执行 `score_regime` 与 `macd_regime` 回测，再串联 `compare + report` 产出对照结果。
- 原因：`score_regime` 已上线但缺少与 `macd_regime` 的标准化对照入口，手工运行多条命令不利于复盘与交接。
- 取舍：模板数量继续增加；但换来可复现的一键专题对照流程，且通过解析测试锁定结构稳定性。

## D-103 策略文档参数区间与模板默认值对齐
- 日期：2026-03-15
- 决策：在 `STRATEGIES.md` 为 `score_regime` 与 `macd_regime` 增加参数建议区间，并显式标注与 `score_vs_macd` 专项模板一致的默认参数值。
- 原因：已有策略说明偏理念层，缺少可执行的调参边界，导致跨会话时容易出现“能运行但参数口径不一致”。
- 取舍：文档维护成本上升；但换来研究模板、策略说明、批处理入口三者口径统一。

## D-104 专项对照模板按资产拆分默认参数
- 日期：2026-03-15
- 决策：新增 `examples/config.batch.score_vs_macd_compare.eth.json`，与 QQQ 版本分离维护，ETH 使用更短窗口和更高阈值的默认参数组合。
- 原因：QQQ 与 ETH 波动结构差异明显，若复用同一默认参数会降低模板可用性，且不利于跨资产对照复盘。
- 取舍：模板维护对象从 1 份变 2 份；但可减少“模板能跑但参数失真”的风险，并通过双模板解析回归控制维护成本。

## D-105 专项模板执行入口优先脚本化
- 日期：2026-03-15
- 决策：新增 `scripts/run_score_vs_macd.py`，统一封装 QQQ/ETH 两套专项模板，支持 `--symbol qqq|eth` 一键执行，默认走 `python -m market_signal_system run-config --file ...`。
- 原因：模板路径较长且有双版本，手工命令容易误用；脚本化入口可降低操作成本并减少路径拼写错误。
- 取舍：增加一个脚本与测试维护负担；但可显著提升日常复盘效率，且通过 `--dry-run` 保持离线可测。

## D-106 报告层内建 score_vs_macd 对照摘要
- 日期：2026-03-15
- 决策：`report` 在“单策略回测”后自动检测同标的 `score_regime` 与 `macd_regime` 工件，新增“策略对照摘要（score_regime vs macd_regime）”并输出 `score_minus_macd` 差值。
- 原因：已有专项模板会产生双策略工件，但没有汇总层时仍需手工对比 JSON；报告自动差值可显著降低复盘成本。
- 取舍：报告段落更长；但逻辑只在工件齐全时触发，不影响普通回测链路。

## D-107 专项脚本优先支持时间窗覆盖与巡检模式
- 日期：2026-03-15
- 决策：`scripts/run_score_vs_macd.py` 新增 `--start/--end` 覆盖模板 `vars`、`--no-run` 仅生成临时配置并打印执行命令。
- 原因：专项模板经常需要改研究区间，直接编辑模板会污染默认参数；同时巡检场景需要先看“最终执行命令 + 渲染配置”再决定是否执行。
- 取舍：脚本参数与分支逻辑变复杂；但保持默认执行路径不变，并通过 CLI 回归测试锁定新契约，风险可控。

## D-108 专项脚本支持报告文件运行时覆盖
- 日期：2026-03-15
- 决策：`scripts/run_score_vs_macd.py` 新增 `--report-file`，运行时改写 batch 中 `report` 任务的 `output_file`。
- 原因：QQQ/ETH 或不同窗口并行实验会重复使用同一模板，若报告文件名固定容易互相覆盖，影响复盘留痕。
- 取舍：脚本需解析并改写模板任务，复杂度上升；但不改动模板默认值，且默认行为完全兼容，风险低。

## D-109 巡检优先支持配置直出
- 日期：2026-03-15
- 决策：`scripts/run_score_vs_macd.py` 增加 `--print-config`，输出渲染后的 batch JSON（含命令行覆盖结果）。
- 原因：仅看到命令不足以审查最终配置，尤其在覆盖 `start/end/report_file` 时需要快速确认任务字段是否按预期落地。
- 取舍：CLI 输出更长；但仅在显式启用时触发，对默认执行与日志噪声无影响。

## D-110 脚本失败语义用子进程回归锁定
- 日期：2026-03-15
- 决策：为 `run_score_vs_macd.py` 增加失败路径子进程回归，模拟解释器非零退出并断言脚本返回码为 1、错误文案稳定。
- 原因：该脚本已成为专项批处理入口，若失败时返回码/文案漂移，会直接影响 CI 与自动化告警可读性。
- 取舍：测试稍微依赖本地可执行脚本权限；但能以最低成本锁定关键故障契约。

## D-111 审计优先支持执行后保留渲染配置
- 日期：2026-03-15
- 决策：`run_score_vs_macd.py` 增加 `--keep-temp-config`，在真实执行路径中可选择不删除临时配置文件。
- 原因：专题跑批出错时需要复盘“当时到底执行了什么配置”，默认自动清理会丢失关键审计证据。
- 取舍：可能增加少量临时文件堆积；但仅在显式开启时保留，默认行为仍自动清理。

## D-112 专项报告命名默认支持标签后缀
- 日期：2026-03-15
- 决策：`run_score_vs_macd.py` 新增 `--report-tag`，可在运行时为 `report.output_file` 追加后缀；未显式传值时自动使用 UTC 时间戳（`YYYYMMDDTHHMMSSZ`）。
- 原因：专题对照经常高频重复执行，固定报告文件名会造成结果覆盖，手工改名成本高且易遗漏。
- 取舍：脚本内部需要解析并改写 report 任务，逻辑复杂度上升；但默认路径兼容不变，仅在显式使用时生效，风险可控。

## D-113 渲染配置落盘优先提供固定路径
- 日期：2026-03-15
- 决策：`run_score_vs_macd.py` 新增 `--emit-config`，允许把渲染后的 batch 配置写入指定路径（相对路径默认相对项目根目录），并直接作为执行输入。
- 原因：临时文件模式不利于审计留痕和跨会话复盘；固定路径可用于 CI 工件归档与手工排障复现。
- 取舍：需要管理额外历史配置文件；但用户可按场景控制目录与命名，且默认仍保持“临时文件+自动清理”路径。

## D-114 报告目录覆盖与文件名覆盖解耦
- 日期：2026-03-15
- 决策：`run_score_vs_macd.py` 新增 `--report-dir`，只替换 `report.output_file` 的目录部分，保留最终文件名；可与 `--report-file` 和 `--report-tag` 叠加。
- 原因：不同环境（本地/CI/批处理）常需要按目录归档结果，但不希望每次手工重写文件名或修改模板默认值。
- 取舍：路径改写逻辑从单点扩展为组合规则（file/tag/dir）；通过脚本回归锁定“先 file/tag，再 dir”顺序，避免行为歧义。

## D-115 参数冲突在 CLI 层前置阻断
- 日期：2026-03-15
- 决策：`run_score_vs_macd.py` 增加参数组合校验，明确拒绝 `--no-run + --dry-run`、`--keep-temp-config + --emit-config`、`--keep-temp-config + --no-run/--dry-run`。
- 原因：这些组合语义冲突或无实际价值，若放行会导致“看似成功但行为不确定”或“执行后才报错”的排障噪声。
- 取舍：入口规则更严格，部分历史宽松用法会被阻断；但错误更早、更确定，整体可维护性更高。

## D-116 渲染配置历史清理采用“时间戳占位符 + 保留窗口”
- 日期：2026-03-15
- 决策：`--emit-config` 支持 `{ts}` 占位符自动注入 UTC 时间戳；新增 `--keep-emitted-configs N`，对同模式文件仅保留最近 N 份，其余移动到 `.trash/score_vs_macd_emitted/`。
- 原因：固定路径覆盖虽可复现，但无法保留历史；纯时间戳落盘会导致工件膨胀。两者结合可同时满足审计留痕与存储可控。
- 取舍：参数语义增加（且 `keep` 仅对含 `{ts}` 的路径有效）；但借助前置校验与回归测试可保持使用体验稳定。

## D-117 报告命名增加批次前缀层
- 日期：2026-03-15
- 决策：`run_score_vs_macd.py` 新增 `--report-prefix`，在 `report.output_file` 文件名前追加统一前缀，并与 `--report-file/--report-tag/--report-dir` 叠加。
- 原因：仅靠 `--report-file` 很难在高频批次中保持稳定命名规范，前缀层可把“批次语义”抽离出来，降低手工改名成本。
- 取舍：文件名改写规则更复杂；通过测试锁定顺序（先 file/prefix/tag，再 dir）并保持默认行为不变，兼顾兼容性与可维护性。

## D-118 渲染配置目录与命名策略解耦
- 日期：2026-03-15
- 决策：`run_score_vs_macd.py` 新增 `--emit-config-dir`，只指定目录，文件名自动生成为 `score_vs_macd_<symbol>_{ts}.json`；并与 `--keep-emitted-configs` 清理策略兼容。
- 原因：`--emit-config` 适合精确路径，但高频巡检时反复手写 `{ts}` 模板成本高且易写错；目录级入口更稳妥、更高效。
- 取舍：参数规则增加（与 `--emit-config` 互斥、与 `--keep-temp-config` 互斥）；但通过 CLI 前置校验和回归测试可保持行为清晰可控。

## D-119 目录级渲染配置增加命名前缀层
- 日期：2026-03-15
- 决策：`run_score_vs_macd.py` 新增 `--emit-prefix`，仅在 `--emit-config-dir` 模式下生效，用于给自动文件名 `score_vs_macd_<symbol>_{ts}.json` 追加前缀。
- 原因：当多个专题共享同一配置落盘目录时，仅靠默认文件名难以快速区分来源；前缀层可把“专题语义”标准化到命名中。
- 取舍：新增一条参数约束（`--emit-prefix` 不能脱离 `--emit-config-dir` 使用）；但入口校验明确，且不影响原有默认命名路径。

## D-120 目录级渲染配置增加命名后缀层
- 日期：2026-03-15
- 决策：`run_score_vs_macd.py` 新增 `--emit-tag`，仅在 `--emit-config-dir` 模式下生效，用于给自动文件名追加批次后缀（如 `..._{ts}_weekly.json`）。
- 原因：同专题在同一目录下高频执行时，只有时间戳不利于按业务批次检索；后缀层可把“批次标签”写入文件名并与前缀语义互补。
- 取舍：参数与校验规则继续增加（`--emit-tag` 不能脱离 `--emit-config-dir`）；但命名规则清晰、兼容默认路径，且已有回归覆盖叠加场景。

## D-121 命名增强必须与历史清理规则兼容
- 日期：2026-03-15
- 决策：保持 `--keep-emitted-configs` 通过“原始命名模板（含 `{ts}`）转 glob”的清理机制，使 `--emit-prefix/--emit-tag` 与历史清理天然兼容。
- 原因：命名增强若破坏清理匹配，会导致渲染配置目录快速膨胀，长期影响可维护性与检索效率。
- 取舍：清理逻辑继续依赖时间戳占位符命名约定；好处是实现简单且已有回归锁定，代价是自定义文件名自由度受限在受控模板内。

## D-122 命名参数阻断路径分隔符
- 日期：2026-03-15
- 决策：`run_score_vs_macd.py` 对 `--emit-prefix/--emit-tag` 新增字符约束，禁止包含 `/` 或 `\\`。
- 原因：命名参数用于“文件名片段”而非路径，若允许分隔符会造成目录穿透风险，并干扰 `keep-emitted-configs` 的 glob 清理匹配。
- 取舍：限制了极少数自定义写法；但换来命名规则更明确、清理行为更可预测、错误更早暴露。

## D-123 score_vs_macd 命名演示模板先沉淀为独立 batch 配置
- 日期：2026-03-15
- 决策：新增 `examples/config.batch.score_vs_macd_naming_demo.json`，固定展示 `batch_prefix + symbol + strategy + batch_tag` 命名规则，并覆盖 `output_tag` 与 `report.output_file`。
- 原因：命名规范只在 README 文字描述不够直观，提供可直接执行的模板能降低协作时“同一实验多种命名风格”导致的检索成本。
- 取舍：示例模板数量增加；但复用 `run-config` 机制、无额外代码路径，维护成本低且审计价值高。

## D-124 目录级渲染命名增加可配置分隔符
- 日期：2026-03-15
- 决策：`run_score_vs_macd.py` 新增 `--emit-separator`（默认 `_`），用于 `--emit-config-dir` 模式下拼接 `emit-prefix/core-name/emit-tag` 的文件名片段。
- 原因：团队命名风格可能偏好 `-` 或其他分隔符，固定下划线会造成脚本输出与现有工件命名体系不一致。
- 取舍：新增一条参数约束（仅在 `--emit-config-dir` 生效，且禁止路径分隔符）；但默认值保持原行为，兼容性风险低。

## D-125 命名演示模板按资产拆分维护
- 日期：2026-03-15
- 决策：新增 `examples/config.batch.score_vs_macd_naming_demo.eth.json`，与 QQQ 版本分离维护，参数集对齐 `score_vs_macd_compare.eth`。
- 原因：若仅保留 QQQ 命名演示，跨资产迁移时仍需手工改参数，容易引入非命名相关差异，影响示例可复现性。
- 取舍：模板文件再增加一份；但结构统一、维护成本可控，且能降低跨资产复制粘贴错误。

## D-126 目录级渲染命名支持模板化但保持强约束
- 日期：2026-03-15
- 决策：`run_score_vs_macd.py` 新增 `--emit-template`，仅与 `--emit-config-dir` 搭配；模板必须包含 `{symbol}` 与 `{ts}`，且与 `--emit-prefix/--emit-tag/--emit-separator` 互斥。
- 原因：部分团队命名规范无法仅靠前后缀和分隔符表达，需要模板化能力；但若过度放开会破坏唯一性与历史清理规则。
- 取舍：新增参数和校验复杂度；通过“占位符必选 + 互斥规则”换取命名灵活性与可维护性的平衡。

## D-127 新增 ATR 归一化策略并纳入默认对比集合
- 日期：2026-03-15
- 决策：新增 `atr_regime`（`trend_strength=(close-trend_ma)/ATR` + `atr_ratio` 分位上限过滤）并直接接入默认对比策略清单、CLI `strategy` choices 与模板入口。
- 原因：现有 `macd_regime/score_regime` 偏向 EMA 与多因子评分，缺少“按波动率归一化趋势强度”的中长线视角；ATR 口径可提升跨资产参数可迁移性。
- 取舍：默认对比任务计算量增加；但仍保持日线中长线范围，且通过定向/全量回归锁定契约，风险可控。

## D-128 ETH 免费数据源采用三级回退而非单一双源
- 日期：2026-03-15
- 决策：在 `BinanceSpotProvider + YahooFinanceProvider` 基础上新增 `CoinGeckoProvider`，ETH 路由升级为 `Binance -> Yahoo -> CoinGecko`。
- 原因：免费 API 在地区、限流与临时故障上存在不确定性，双源在极端情况下仍可能同时不可用；增加第三源可提升批处理连续性。
- 取舍：CoinGecko 通过成交价序列重采样生成 `OHLCV`，精度低于原生 K 线；但作为最后回退源可接受，优先保障“可获取数据”与主链路可运行性。

## D-129 新策略默认配套稳定性批处理模板
- 日期：2026-03-15
- 决策：为 `atr_regime` 增加标准化流水线模板 `config.batch.atr_regime_stability.json`，固定 `research -> stability -> report` 三段式流程。
- 原因：策略新增后若只提供单次 backtest/research 模板，团队容易遗漏稳定性评估；批处理模板可把“稳健性复验”变成默认动作。
- 取舍：示例模板数量继续增加；但全部复用 `run-config` 既有能力且有解析回归保护，维护成本可控。

## D-130 emit 命名校验与拼接逻辑下沉为共享工具
- 日期：2026-03-15
- 决策：新增 `src/market_signal_system/utils/emit_naming.py`，统一承载 `run_score_vs_macd.py` 的 `emit-prefix/tag/template/separator` 校验与文件名拼接逻辑。
- 原因：脚本内已有多轮命名参数增强，继续叠加会导致验证与拼接分散、重复代码增加；下沉工具后更便于复用与回归覆盖。
- 取舍：增加一个工具模块与少量导入复杂度；但脚本职责更聚焦，后续新增命名参数时修改面更小、行为更可控。

## D-131 命名演示脚本默认走安全巡检路径
- 日期：2026-03-15
- 决策：`run_score_vs_macd.py` 新增 `--naming-demo`，切换到命名演示模板时若未显式指定 `--no-run/--dry-run`，默认自动启用 `--no-run`。
- 原因：命名演示主要用途是校验 `output_tag/report` 规则，不应默认触发完整批处理执行；默认只渲染可避免误跑长任务。
- 取舍：与“脚本默认执行”行为相比更保守，首次使用需显式理解该模式；但能显著降低误操作成本，符合巡检优先目标。

## D-132 命名演示执行需显式确认
- 日期：2026-03-15
- 决策：为 `run_score_vs_macd.py` 增加 `--run` 开关，仅在命名演示场景下用于显式覆盖默认 `--no-run`；并与 `--no-run/--dry-run` 互斥。
- 原因：`--naming-demo` 默认安全模式能防误执行，但仍需保留“同命令直接执行”的能力；显式 `--run` 可让执行意图在命令层自解释。
- 取舍：CLI 参数再增加一项，学习成本略增；但换来巡检与执行模式边界更清晰，减少线上误触发批处理风险。

## D-133 单策略回测默认引入买入持有基准对比
- 日期：2026-03-15
- 决策：`BacktestEngine` 默认计算同标的买入持有基准收益，并在 `metrics` 增加 `benchmark_total_return/excess_total_return/information_ratio/active_max_drawdown` 字段；`report` 自动展示这些字段（若存在）。
- 原因：仅看策略绝对收益会掩盖“跑输基准仍看起来盈利”的问题；中长线研究需要先确认策略是否优于最朴素的持有方案。
- 取舍：指标输出字段增加，历史工件的展示格式略有扩展；但保持向后兼容（旧工件无该字段时报告自动降级），并通过回归测试锁定计算契约。

## D-134 排行榜评分纳入超额收益维度
- 日期：2026-03-15
- 决策：`compare` 评分新增 `score_excess_return`（基于 `excess_total_return` 排名），并把综合评分权重调整为 `sharpe 30% + calmar 20% + total_return 20% + excess_return 20% + drawdown 10%`。
- 原因：仅按绝对收益评分会放大高 beta 策略在单边行情中的优势，无法体现“是否真正跑赢买入持有基准”。
- 取舍：与历史排行榜分数口径不完全可比；但新口径更贴近中长线策略筛选目标，并保持字段兼容（只新增列、不删除旧列）。

## D-135 组合回测基准统一为同窗等权持有
- 日期：2026-03-15
- 决策：`run_portfolio_backtest_detailed` 默认增加“同窗口等权持有基准”（`1/N` 持有、t-1 执行、同成本扣减），并复用统一指标字段 `benchmark_total_return/excess_total_return/information_ratio`。
- 原因：单策略已引入基准对比，组合层若缺失会导致研究口径割裂，难以横向比较“策略增强是否真的创造超额收益”。
- 取舍：组合 metrics 字段增多，报告行更长；但保持兼容（旧工件自动降级展示），且统一指标命名可降低下游分析复杂度。

## D-136 对比排行榜拆分“全局混排”与“按标的分组”
- 日期：2026-03-15
- 决策：`compare` 在保留 `leaderboard_<symbols>.csv` 的同时，新增 `leaderboard_by_symbol_<symbols>.csv`，按 `symbol` 分组排序并输出 `symbol_rank/symbol_strategy_count`。
- 原因：全局混排适合快速筛选全局最优，但在多资产场景容易误导“同资产策略优劣”判断；分组工件可直接支持组内比较，减少人工二次处理。
- 取舍：输出工件增加一份；但实现仅复用已生成 leaderboard 做后处理，不增加回测计算成本，兼容性风险低。

## D-137 报告排行榜增加超额收益 Top/Bottom 双层摘要
- 日期：2026-03-15
- 决策：`report` 在“多策略排行榜”段落新增每文件 `excess_top/excess_bottom`，并追加跨文件 `aggregate_excess_top/aggregate_excess_bottom`。
- 原因：排行榜综合分数已纳入超额收益，但复盘时仍需快速看到“谁最能跑赢/跑输基准”；双层摘要可减少人工筛表时间。
- 取舍：报告文本长度增加；但字段来源完全基于现有 `leaderboard_*.csv`，无需新增计算链路，风险低且可回归验证。

## D-138 分组排行榜摘要直接展示组内冠军
- 日期：2026-03-15
- 决策：`report` 新增“分组排行榜（按标的）”段落，读取 `leaderboard_by_symbol_*.csv` 并输出每个 symbol 的冠军策略与分数。
- 原因：仅落盘分组 CSV 仍需人工过滤 `symbol_rank==1`，批量复盘效率偏低；报告直接给冠军可快速定位“每个资产当前最优策略”。
- 取舍：报告新增一段文本；但逻辑仅消费已有工件，不改变回测/排行主链路，风险可控且易回归。

## D-139 run-config 摘要纳入 compare 组内冠军快照
- 日期：2026-03-16
- 决策：`run-config` 在 `compare` 任务完成后，将 `leaderboard_by_symbol_*` 的冠军信息写入摘要 JSON 顶层 `symbol_group_champions`，并在任务结果行追加 `symbol_group_champion_count/symbol_group_champions` 字段。
- 原因：此前批处理摘要只有任务成败与耗时，缺少“每次 compare 的组内冠军是谁”这一核心巡检信号，仍需人工打开 CSV 二次筛选。
- 取舍：摘要字段体积增加（尤其多 symbol 场景）；但仅在 compare 任务存在时新增，CSV 仍保持可读，且通过回归测试锁定字段契约。

## D-140 分组冠军稳定性默认按最近 10 份榜单统计
- 日期：2026-03-16
- 决策：`report` 新增“分组冠军稳定性”段，按最近 10 份 `leaderboard_by_symbol_*`（按文件时间）统计每个 symbol 的 `samples/switches/unique_champions/latest`。
- 原因：只有“当前冠军”无法反映策略是否频繁切换；中长线巡检更关注冠军是否稳定，切换过快通常意味着参数或市场状态漂移。
- 取舍：报告段落变长，且样本窗口固定为 10；但口径简单稳定、可回归验证，后续可在不破坏现有字段的前提下扩展阈值告警。

## D-141 批处理聚合增加冠军切换趋势
- 日期：2026-03-16
- 决策：`report` 在“批处理执行摘要”聚合段新增 `champion_switch_last_N_runs`，基于最近 N 次 run 的 `symbol_group_champions` 统计各 symbol 冠军切换次数。
- 原因：文件级分组稳定性反映单次输出，仍缺“跨 run 连续巡检”视角；将切换趋势挂到 run-config 聚合可直接服务日常批处理巡检。
- 取舍：仅在摘要携带 `symbol_group_champions` 时输出，老工件会自动降级为空；新增一行聚合文本但不引入额外工件类型，兼容成本低。

## D-142 冠军稳定性告警分级与 CSV 工件
- 日期：2026-03-16
- 决策：为“分组冠军稳定性”和 `champion_switch_last_N_runs` 增加 `alert=none/watch/high` 分级；`report` 默认额外导出 `champion_switch_last_runs.csv`，用于外部监控消费。
- 原因：仅输出 `switches` 需要人工判读，不利于自动巡检；告警分级与结构化 CSV 可直接接入定时任务/监控系统。
- 取舍：`report` 参数与输出工件增加；但默认值保持兼容（阈值默认 `watch=1/high=2`，CSV 可通过空字符串关闭），收益大于复杂度。

## D-143 冠军趋势 CSV 增加时序字段
- 日期：2026-03-16
- 决策：`champion_switch_last_runs.csv` 新增 `first_run_time` 与 `latest_run_time` 字段，记录最近窗口内该 symbol 首次/最近一次参与统计的 run 时间。
- 原因：外部监控系统需要直接基于工件构建时间序列；仅有 `samples/switches` 难以确定切换发生区间。
- 取舍：CSV 字段增加但保持向后兼容；报告正文格式不变，避免影响现有人工巡检习惯。

## D-SIGNAL-PILOT-MVP 观察期通知跟踪优先级
- 日期：2026-03-16
- 决策：将“信号通知 + 通知时点落盘 + 观察期收益跟踪 + 观察报告”作为独立 MVP 功能优先实现，先采用本地 CLI + ledger 持久化方案，不先做重 Web UI。
- 原因：用户核心需求不是只看历史回测，而是先纸上观察 1~2 个月真实信号表现，再决定是否进入实盘；因此需要一条独立于历史回测的 signal pilot 链路。
- 取舍：本轮优先实现 scan/update/report 与台账去重/关闭逻辑，消息渠道先抽象、后接入。

## D-143 MVP 验收链路收敛为单命令
- 日期：2026-03-16
- 决策：新增 `mvp` CLI 命令，将“QQQ/ETH 数据接入 + 至少 3 个策略回测 + 单标的模拟交易 + 摘要落盘”收敛到一次执行。
- 原因：虽然系统已具备分步命令，但首次接入或回归验收时仍需手动串联多条命令；单命令能显著降低上手与验收成本。
- 取舍：`mvp` 逻辑会与 `backtest/simulate` 有少量流程复用而非完全复用 CLI 输出命名；但通过独立 `mvp_*` 工件前缀避免覆盖既有研究产物，兼容风险较低。

## D-144 MVP 摘要后处理独立为脚本
- 日期：2026-03-16
- 决策：新增 `scripts/mvp_report.py`，将 `mvp_summary_*.json` 后处理为“按标的分组排行榜 + Markdown 简报”，不耦合进主 CLI。
- 原因：`mvp` 命令职责是“产出标准化工件”，而报表排序属于后处理视图；独立脚本可减少主 CLI 复杂度并支持离线重复生成。
- 取舍：新增一个脚本入口与测试维护成本；但输出格式更明确，且可以基于历史 summary 重跑，不需要重新回测。

## D-145 MVP 日常巡检先采用 run-config 批处理模板
- 日期：2026-03-16
- 决策：新增 `examples/config.batch.mvp_daily.json`，默认串联 `mvp -> report`，作为每日基线巡检入口。
- 原因：相比单次手工执行多条命令，批处理模板更容易固化为定时任务与团队共享流程。
- 取舍：当前模板未直接调用 `mvp_report.py`（仍需脚本单独执行），但先保证主链路完全走 `run-config`，后续再视需求合并更多后处理步骤。

## D-144 Signal Pilot 台账先用 CSV 持久化
- 日期：2026-03-16
- 决策：Signal Pilot 的 alert ledger 首版采用 `data/state/signal_alert_ledger.csv`，统一字段覆盖 alert 生命周期（open/closed/expired + 收益/MFE/MAE/持有时长）。
- 原因：MVP 阶段优先简单、稳定、可调试；CSV 与现有项目工件风格一致，便于人工排查和脚本处理。
- 取舍：并发写入能力弱于数据库；但当前链路为本地低频 CLI，CSV 的维护成本最低且足够可用。

## D-145 新信号去重口径按“同标的+同策略+同参数+同方向+open”
- 日期：2026-03-16
- 决策：`scan-alerts` 仅在不存在同向 open alert 时创建新 alert；flat 信号不创建 alert；新 alert 同步写入本地 JSONL 待通知队列。
- 原因：用户核心是“避免同一方向重复提醒”，并保留 long/short 切换时的新机会记录。
- 取舍：不会在 scan 阶段自动关闭反向旧单（交由 update 阶段处理）；实现更稳妥、职责更清晰。

## D-146 观察收益更新与关闭规则先做最小闭环
- 日期：2026-03-16
- 决策：`update-alerts` 每次刷新 open alert 的当前收益、MFE/MAE、持有时长，并按“反向信号 -> closed、超时 -> expired”关闭。
- 原因：这两条规则能覆盖绝大多数中长线观察场景，且无需引入额外复杂风控参数即可形成闭环。
- 取舍：暂未引入止盈止损/手动标签等高级关闭条件；但先保证核心观察链路可运行、可复现。

## D-147 观察报告默认输出 7/30/60 三窗口
- 日期：2026-03-16
- 决策：`report-alerts` 默认输出 7/30/60 天窗口，按 `symbol+strategy` 汇总信号数、胜率、平均收益、最大盈亏、平均持有、MFE/MAE，并同时导出 CSV/JSON/Markdown。
- 原因：7/30/60 天与“先观察 1~2 个月再决定实盘”的需求直接匹配，且可同时满足机器消费和人工阅读。
- 取舍：窗口固定会增加部分短期策略噪音；但可通过命令行 `--windows` 覆盖，兼顾默认标准化与灵活性。

## D-148 MVP 摘要内建跨策略 TopN
- 日期：2026-03-16
- 决策：`mvp_summary` 默认增加 `backtest_topn_by_symbol`（每个 symbol 的 Top3）与 `backtest_topn_overall`（全局 Top5），排序键为 `total_return`，并以 `sharpe` 作为同收益下的次级排序。
- 原因：MVP 一键跑通后最常见问题是“哪个策略最优”；将 TopN 直接写入摘要能避免每次都再写解析脚本。
- 取舍：摘要 JSON 体积略增；但字段是纯增量，兼容旧消费者，且可显著降低日常巡检的人力成本。

## D-149 Signal Pilot 模板采用“单任务 + 批处理”双形态
- 日期：2026-03-16
- 决策：新增 `examples/config.signal_pilot.scan/update/report.json` 三个单任务模板，以及 `examples/config.batch.signal_pilot.daily.json` 串联模板。
- 原因：Signal Pilot 既需要“单命令排查/调试”，也需要“定时批处理巡检”；双形态模板能同时覆盖这两类使用场景。
- 取舍：示例配置文件数量增加；但模板结构简单且有解析回归保护，维护成本可控。

## D-150 每日基线模板采用平铺任务链路
- 日期：2026-03-16
- 决策：新增 `examples/config.batch.baseline.daily.json`，按 `mvp -> scan-alerts -> update-alerts -> report-alerts -> report` 平铺串联，不使用 batch 嵌套。
- 原因：`run-config` 明确禁止 `batch` 递归；平铺链路能在不改主逻辑的前提下实现每日基线巡检一键执行。
- 取舍：模板任务数更多、可读性略降；但行为最稳定，避免新增执行语义分支。

## D-149 QQQ 增加 Stooq 回退源
- 日期：2026-03-16
- 决策：`DataManager` 中 `QQQ` 路由从单一 Yahoo 改为 `Yahoo -> StooqDailyProvider` 级联回退，Stooq 使用公开 CSV 日线接口。
- 原因：QQQ 属于 MVP 必备标的，若仅依赖 Yahoo 单源，一旦接口抖动会直接影响回测与模拟链路可用性。
- 取舍：Stooq 先仅支持 `1d` 频率，不覆盖小时级别；但保持实现简单并与中长线默认频率一致，足以作为稳定兜底。

## D-150 simulate 默认导出结构化 summary
- 日期：2026-03-16
- 决策：`simulate` 命令默认新增 `outputs/sim_summary_<symbol>_<strategy>.json`，并提供 `--summary-file` 覆盖输出文件名。
- 原因：仅依赖 CSV 需要二次拼装才能获取“最新账户状态”；summary 直接沉淀快照字段，便于 run-config/监控脚本直接消费。
- 取舍：输出工件数量增加 1 个文件；但字段与 `broker.snapshot()` 一致、成本低且不破坏现有 CSV 消费链路。

## D-151 Signal Pilot 模板路径使用“文件名相对语义”
- 日期：2026-03-16
- 决策：`examples/config.signal_pilot.*.json` 中 `ledger_file/notification_file` 统一改为文件名（如 `signal_alert_ledger.csv`），不再写 `data/state/...` 前缀。
- 原因：Signal Pilot 内部会将相对路径映射到默认目录（`STATE_DIR` 或 `OUTPUT_DIR`）；若模板再写目录前缀会被二次拼接到错误位置。
- 取舍：模板与旧路径写法不兼容；但行为更符合 CLI 默认约定，避免静默写错目录导致巡检难排障。

## D-152 baseline-summary 作为每日巡检去重层
- 日期：2026-03-16
- 决策：新增 `baseline-summary` 命令，聚合 `mvp_summary`、`signal_pilot_alert_report` 与 `report_index`，统一产出 `baseline_daily_digest.json/csv`。
- 原因：`baseline daily` 链路中同类指标分散在多个工件，人工阅读有重复与漏看风险；需要一个轻量去重视图作为单一入口。
- 取舍：当前优先做“聚合与扁平导出”，不在本轮引入阈值告警与外发通知；先保证可复现与可读性，后续再接入告警策略。

## D-153 Signal Pilot 通知派发先落地本地可追踪闭环
- 日期：2026-03-16
- 决策：新增 `dispatch-alerts`，默认从 `signal_pilot_notifications.jsonl` 读取待通知事件，支持两种通道：本地 sink（默认）与可选 webhook；成功后回写 ledger 的 `notification_sent/notification_channel`。
- 原因：在不引入外部消息平台依赖的前提下，需要先形成“可派发、可审计、可恢复”的通知最小闭环。
- 取舍：本轮未实现 webhook 重试与去重窗口，仅保证单次执行内幂等（已发送事件跳过）；后续再补失败重试与告警升级策略。

## D-154 baseline daily 日期参数化与自动渲染执行
- 日期：2026-03-16
- 决策：将 `config.mvp.json` 与 `config.signal_pilot.*.json` 改为 `vars` 占位符输入（`start/end/as_of`），并新增 `scripts/run_baseline_daily.py` 负责按运行日渲染 `config.batch.baseline.daily.json` 后执行。
- 原因：模板硬编码日期会导致定时任务维护成本高且容易遗漏；参数化 + 渲染脚本可保证日常巡检可重复、可自动化。
- 取舍：增加了一层“渲染后临时配置”的中间工件；但可显式审计执行参数，并支持 `--no-run` 先巡检再执行。

## D-155 run-config 配置格式扩展到 YAML
- 日期：2026-03-16
- 决策：`run-config` 在保留 JSON 的同时，新增 `.yaml/.yml` 配置解析能力；默认按文件后缀判定解析器（JSON 或 YAML）。
- 原因：批处理模板规模持续增长，JSON 可读性与注释能力不足；YAML 更适合中长线研究参数模板维护。
- 取舍：新增 `PyYAML` 依赖与解析路径；通过“仅在 `.yaml/.yml` 走 YAML，其余仍走 JSON”控制行为稳定性，并保持原有 JSON 模板完全兼容。

## D-156 YAML 模板按“JSON 等价副本”策略落地
- 日期：2026-03-16
- 决策：新增 `examples/config.mvp.yaml` 与 `examples/config.batch.mvp_daily.yaml`，字段与现有 JSON 模板保持一一对应，不引入 YAML 专属字段。
- 原因：当前目标是“降低维护成本并保持运行稳定”，等价副本策略最易验证且不会引入语义分叉。
- 取舍：模板文件数量增加；但可通过回归测试锁定 JSON/YAML 解析结果一致，后续再按需要扩展 YAML 专属注释与组织方式。

## D-157 run-config 对已支持命令启用最小 schema 校验
- 日期：2026-03-16
- 决策：`run-config` 在解析已支持命令时，前置执行最小 schema 校验：拦截未知字段、缺失必填字段、类型不匹配与非法 choices；未知命令仍保留原语义，交由批处理执行阶段进入失败统计/重试路径。
- 原因：近期配置模板与 YAML 使用量上升，拼写或类型错误常在任务运行时才暴露，排障成本高；需要在解析期尽早失败且错误信息可读。
- 取舍：新增了基于 CLI parser 的轻量校验逻辑与测试维护成本；但避免手写重复 schema，并保持 `on_error=continue`、重试预算等既有行为不回归。

## D-158 Signal Pilot/Baseline 批处理模板补齐 YAML 等价版本
- 日期：2026-03-16
- 决策：新增 `examples/config.batch.signal_pilot.daily.yaml` 与 `examples/config.batch.baseline.daily.yaml`，与 JSON 模板保持字段等价，不引入新语义。
- 原因：已有 `run-config` YAML 支持，但核心日常巡检链路（Signal Pilot/Baseline）仍仅有 JSON 模板，影响团队在 YAML 统一维护时的一致性。
- 取舍：模板文件数量继续增加；但通过解析回归锁定契约，避免 JSON/YAML 行为漂移。

## D-159 run-config 最小 schema 增补常见数值边界
- 日期：2026-03-16
- 决策：在已支持命令的 schema 校验中增加常见数值边界约束（如 `fee_rate/slippage_bps/min_quantity >= 0`、`bars_per_year/lookback_days/max_events > 0`、`allocation_per_signal ∈ (0,1]`、若适用则 `max_drawdown` 等比例字段在 `[0,1]`）。
- 原因：仅有类型校验仍会放过明显非法配置，导致运行到策略/执行层才失败，排障链路长。
- 取舍：边界规则保持“最小且保守”，只覆盖跨命令高频字段；避免一次性引入大量业务约束导致旧模板回归风险。

## D-160 Signal Pilot 文档采用“独立说明 + README 导航”结构
- 日期：2026-03-16
- 决策：新增独立文档 `SIGNAL_PILOT.md`，并在 `README.md` 顶部增加导航入口，不将完整说明直接堆叠到 README。
- 原因：Signal Pilot 已形成独立生命周期与数据台账语义，若全部放在 README 会稀释核心启动信息并提高维护冲突概率。
- 取舍：文档文件增加 1 个，需要同步维护；但职责边界更清晰，便于后续单模块持续迭代。

## D-161 Signal Pilot 派发链路先落地“可重试 + 可恢复幂等”最小闭环
- 日期：2026-03-16
- 决策：`dispatch-alerts` 增加 `retry_count/retry_delay_ms` 与 `idempotency_window_minutes`；并在 ledger 持久化 `notification_last_attempt_at/notification_last_sent_at/notification_fail_count`。
- 原因：仅依赖 `notification_sent` 布尔字段不足以覆盖 webhook 抖动场景，会导致“失败后盲重试”或“重复派发”两类风险；需要在本地先建立可恢复的去重与失败追踪语义。
- 取舍：状态字段与参数复杂度上升；但默认值保持兼容（不强制重试、不开启窗口），并通过回归测试锁定行为，风险可控。

## D-162 baseline-summary 默认接入派发失败健康度
- 日期：2026-03-16
- 决策：`baseline-summary` 默认读取 `signal_alert_ledger.csv`，新增 `signal_dispatch` 指标（open/pending/failed/failed_ratio）并支持阈值告警（`dispatch_max_failed_count/dispatch_max_failed_ratio`）。
- 原因：Signal Pilot 的通知风险此前只在 `dispatch` 命令输出可见，未进入每日去重摘要；值班巡检难以及时识别“通知通道异常”。
- 取舍：`baseline-summary` 对 ledger 文件产生软依赖；但采用“文件缺失即降级为空指标”策略，不影响主链路可运行性。

## D-162 baseline-summary 告警先采用“内建阈值 + 可选队列导出”
- 日期：2026-03-16
- 决策：在 `baseline-summary` 直接内建阈值告警（Signal 窗口信号数/胜率/平均收益 + 模拟累计收益），并通过 `--emit-alert-queue` 可选导出 `baseline_summary_alerts.jsonl`。
- 原因：日常巡检需要先有“结构化告警产物”再谈自动派发；把阈值与摘要同源计算可降低口径漂移，且无需引入新服务即可接入现有派发链路。
- 取舍：`baseline-summary` 参数增加、默认输出结构变大；但阈值默认值保守，且队列导出默认关闭，兼容现有使用方式。

## D-163 baseline 告警模板采用“独立单任务 JSON/YAML”双版本
- 日期：2026-03-16
- 决策：新增 `examples/config.baseline.summary.alerts.json/.yaml`，采用单任务 `baseline-summary` 模板，显式开启阈值参数与 `emit_alert_queue`。
- 原因：告警模板与每日大流水线职责不同，独立模板可用于快速重跑、回放和告警参数调试，避免每次都跑完整 batch。
- 取舍：模板文件数量增加；但通过 JSON/YAML 等价模板和解析回归锁定行为，维护成本可控。

## D-164 baseline 告警派发采用“独立 batch 模板串联”而非改动默认 daily 模板
- 日期：2026-03-16
- 决策：新增 `examples/config.batch.baseline.alerts.daily.json/.yaml`，在不改动默认 `config.batch.baseline.daily.*` 的前提下，提供 `baseline-summary -> dispatch-alerts` 串联链路。
- 原因：默认 daily 模板已用于基础验收，直接改默认行为会影响既有流程稳定性；独立模板更利于灰度验证告警阈值与派发策略。
- 取舍：模板数量进一步增加；但将“基础链路”和“告警链路”隔离后，兼容性和运维可控性更高。

## D-165 新策略优先选择 dual_momentum
- 日期：2026-03-16
- 决策：在既有中长线策略集上新增 `dual_momentum`（短周期+长周期动量共振，叠加长期趋势过滤与波动率过滤）。
- 原因：当前策略集中已有单动量与评分策略，但缺少“多周期动量一致性”这一稳健信号形态；该策略可在趋势确认和交易频率之间提供新的中间选项。
- 取舍：过滤条件更严格，可能牺牲拐点早期收益；优先保证信号稳健性和可解释性。

## D-166 CLI 策略白名单集中管理
- 日期：2026-03-16
- 决策：在 `cli.py` 增加 `STRATEGY_CHOICES` 与 `COMPARE_DEFAULT_STRATEGIES` 常量，统一供 `backtest/simulate/research/compare` 等命令复用。
- 原因：此前策略列表在多个参数定义中重复，新增策略容易遗漏并引入不一致。
- 取舍：集中常量提升一致性，但会让 `cli.py` 对策略注册变化更敏感；通过回归测试约束行为。

## D-167 周频采用“日线重采样”而非新增外部周线源
- 日期：2026-03-16
- 决策：`DataManager.get_history(..., interval="1wk")` 统一改为先取日线（含缓存/增量/多源回退），再本地聚合为 `W-FRI` 周线 OHLCV。
- 原因：免费数据源的原生周线口径差异较大且稳定性不一；复用日线链路可最大化一致性与可复现性。
- 取舍：周线请求会先拉取日线，短期增加少量计算开销；但避免维护额外 provider 分支，工程复杂度更低。

## D-168 周频先以 compare 模板形式落地
- 日期：2026-03-16
- 决策：新增 `examples/config.compare.weekly.json`，默认覆盖 `QQQ+ETH` 与现有策略集，`interval=1wk` 直接复用 `run-config`。
- 原因：周频能力上线后，需要一个可复现实验入口快速验证端到端链路（解析 -> 拉数 -> 回测对比）。
- 取舍：先提供 JSON 模板，不同步扩展 YAML 版本；优先控制模板维护成本，待使用频次上升再补齐。

## D-169 周频链路优先补离线子进程冒烟
- 日期：2026-03-16
- 决策：新增 `test_compare_cli_weekly_interval_runs_via_subprocess_without_network`，覆盖 `python -m market_signal_system compare --interval 1wk` 在无 `PYTHONPATH` 下的可执行性。
- 原因：周频能力依赖“日线缓存 + 重采样”路径，单元测试不足以覆盖 CLI 入口与缓存文件契约，需补子进程级回归防止主链路回退。
- 取舍：测试会临时备份并替换 `QQQ_1d.csv/ETH_1d.csv`，实现稍复杂；但可稳定验证离线环境可运行。

## D-170 多账号隔离优先落地 SQLite 单机模式
- 日期：2026-03-16
- 决策：新增 SQLite 存储层，默认 `data/state/market_signal_system.db`，`account_id` 作为核心命名空间键；先不引入登录系统或远程数据库。
- 原因：当前需求核心是“本地多账号隔离 + 可持续演进”；SQLite 依赖最少、迁移成本低、便于后续升级。
- 取舍：并发能力与跨机共享有限；但满足当前单机任务优先级，且 schema 可平滑迁移到 MySQL/PostgreSQL。

## D-171 Signal Pilot 持久化采用“SQLite 优先 + CSV 兼容镜像”
- 日期：2026-03-16
- 决策：`load/save/upsert_alert_rows` 优先读写 SQLite，并同步写 CSV；旧 CSV 在对应账号库为空时自动导入 SQLite。
- 原因：需要在不破坏现有基线脚本与报告链路的前提下完成存储升级。
- 取舍：短期存在双写成本；但兼容风险最低，可逐步将下游读链路切换到 SQLite。

## D-172 CLI 先聚焦 Signal Pilot 四命令的 account_id/db_file
- 日期：2026-03-16
- 决策：`scan-alerts/dispatch-alerts/update-alerts/report-alerts` 统一增加 `--account-id` 与 `--db-file`，并将 `account_id` 写入摘要返回。
- 原因：Signal Pilot 是当前最活跃的数据写入面，优先隔离可立即提升安全性与可维护性。
- 取舍：先不全面改造所有命令；模拟链路仅补充状态元数据入库，后续再扩展到账户级交易日志。

## D-173 模拟状态文件按账号命名空间自动隔离
- 日期：2026-03-16
- 决策：当 `account_id != default` 且 `state_file` 使用默认名时，自动重写为 `<account_id>__paper_broker.json` / `<account_id>__paper_portfolio.json`，并同步写入 `sim_state_meta`。
- 原因：避免多账号共用默认状态文件导致覆盖，且便于从数据库反查状态文件。
- 取舍：默认文件名在多账号场景发生变化；但用户可显式传入 `--state-file` 覆盖，兼容可控。

## D-167 多标的数据对齐下沉到 DataManager
- 日期：2026-03-16
- 决策：新增 `DataManager.get_aligned_history(symbols, ...)`，统一输出“共同时间轴对齐后的多标的 OHLCV”。
- 原因：组合回测与组合模拟均需要时间轴对齐，分散在调用层重复实现容易出现行为漂移与维护负担。
- 取舍：DataManager 责任边界扩大，但换来跨模块一致性与更低重复代码。

## D-168 组合链路保留旧 DataManager 兼容回退
- 日期：2026-03-16
- 决策：`portfolio` 与 `simulate-portfolio` 在调用对齐接口时增加 `hasattr(..., "get_aligned_history")` 回退路径。
- 原因：现有测试中广泛使用 fake/stub data manager，仅实现 `get_history`；直接强依赖新接口会导致大量回归失败。
- 取舍：调用层多一层兼容分支，但可以平滑迁移并保持旧测试/扩展点稳定。

## D-174 Web 登录优先采用“预置账号 + 会话 Cookie”最小闭环
- 日期：2026-03-16
- 决策：先交付不含页面注册的网页登录 MVP：后台脚本预置账号、`/login` 表单登录、`HttpOnly` session cookie、`/signal-pilot` 账号隔离展示。
- 原因：当前核心诉求是“能登录 + 能隔离多账号数据 + 可快速部署”，优先闭环比先做完整用户系统更稳妥。
- 取舍：暂不支持自助注册、找回密码、OAuth；通过后台脚本满足当前运维流程。

## D-175 数据访问统一收敛到 Repository/Service 抽象
- 日期：2026-03-16
- 决策：新增 `appdb` 分层（`models/repositories/services/sqlite_backend`），Web 与业务逻辑仅依赖 Service，不直接写 SQL。
- 原因：需要确保未来从 SQLite 迁移到 PostgreSQL/MySQL 时，不必改动上层业务与路由代码。
- 取舍：初期代码量上升，但长期维护性和可迁移性显著提升。

## D-176 SQLite Schema 采用“兼容迁移优先”策略
- 日期：2026-03-16
- 决策：`SQLiteSchemaManager` 在建表后执行列存在性检查与补列（如 `accounts.username/is_active`），并补数据回填后再建索引。
- 原因：仓库中已有历史 SQLite 文件与旧表结构，直接按新表假设建索引会导致迁移失败。
- 取舍：迁移逻辑复杂度增加，但能保障旧环境平滑升级。

## D-177 Signal Pilot Web 展示只读化，先不改动交易主链路
- 日期：2026-03-16
- 决策：Web MVP 阶段只做 Signal Pilot 数据只读展示，不在页面直接触发 `scan/update/dispatch`。
- 原因：先保证鉴权和多账号隔离正确，避免把任务调度与写操作一并引入导致安全面和复杂度暴涨。
- 取舍：页面交互能力有限；通过 CLI 与批处理继续承担写链路，后续再按权限模型扩展。

## D-178 Web 部署先提供“模板化最小运维件”
- 日期：2026-03-16
- 决策：新增 `.env.web.example`、`deploy/systemd/mss-web.service.example`、`deploy/nginx/mss-web.nginx.conf.example`，并让 `run_web.py` 支持环境变量默认值。
- 原因：当前目标是“可部署 MVP”，先给可直接改值落地的模板能最快降低上线门槛。
- 取舍：模板为示例级，不覆盖容器编排与高可用；后续按实际部署环境再扩展。

## D-179 Web 鉴权接口先采用“Session Cookie + 只读 API”组合
- 日期：2026-03-16
- 决策：在登录页与 Dashboard 之外，补充 `GET /api/me` 与 `GET /api/signal-pilot/alerts` 两个只读鉴权接口，统一复用同一 session cookie 鉴权逻辑。
- 原因：前端页面之外还需要可自动化验证的接口（测试与运维巡检）；先提供只读接口可在不扩大写操作风险面的前提下验证账号隔离与会话有效性。
- 取舍：当前不开放 Web 写接口（scan/update/dispatch 仍走 CLI/批处理），交互能力有限；但安全边界更清晰，后续可按权限模型逐步扩展。

## D-180 Web 启动入口保持“脚本直跑优先”
- 日期：2026-03-16
- 决策：`admin_seed_account.py` 与 `run_web.py` 继续保持“仓库根目录直接执行”路径，README 统一以 `python3 scripts/...` 作为默认命令（不要求手动设置 `PYTHONPATH`）。
- 原因：值班与运维场景更依赖复制即运行的命令，减少环境变量前置可显著降低误用概率。
- 取舍：脚本内部需维护一次 `src/` 注入逻辑；但换来更稳定的启动体验，尤其适合 cron/systemd 场景。

## D-180 baseline-summary 按账号聚合并引入 SQLite 派发健康度
- 日期：2026-03-16
- 决策：`baseline-summary` 增加 `account_id/db_file/dispatch_window_days`，默认按账号读取台账；派发健康度改为聚合 SQLite `notification_dispatch_records`（窗口内尝试数/失败率/重试命中率），并保留原台账口径。
- 原因：多账号场景下单看 CSV 无法完整反映派发链路质量，且缺乏可追溯的重试行为统计。
- 取舍：摘要结构新增 `signal_dispatch_health`，下游解析方需要兼容新字段；通过保留 `signal_dispatch` 维持旧报表兼容。

## D-181 Signal Pilot 多账号示例优先提供“显式模板”而非动态参数脚本
- 日期：2026-03-16
- 决策：新增 `config.batch.signal_pilot.daily.default/prod/research.json` 三份模板，直接写明 `account_id`、队列文件与报告前缀。
- 原因：运维与值班场景更需要“开箱即跑、路径可见”的模板，显式文件比动态拼接参数更不易误配。
- 取舍：模板数量增加；但命名空间边界更清晰，后续可再统一到脚本层做参数化入口。

## D-182 周频 compare 入口采用“渲染模板 + run-config”脚本复用
- 日期：2026-03-16
- 决策：新增 `scripts/run_compare_weekly.py`，通过覆盖 `start/end/symbols/strategies` 渲染 `examples/config.compare.weekly.json` 后调用 `run-config` 执行。
- 原因：相比新增独立 CLI 子命令，脚本复用现有配置解析与执行链路，变更面更小、可维护性更高，也便于 cron/heartbeat 直接调用。
- 取舍：多一层渲染文件管理；通过 `--no-run` 与自动清理临时文件降低运维负担。

## D-183 baseline 摘要先以只读 API 接入 Web（不改交易写链路）
- 日期：2026-03-16
- 决策：新增 `GET /api/baseline-summary/latest`，按登录账号读取 `outputs/baseline_daily_digest*.json` 中匹配 `account_id` 的最新摘要；不匹配则返回 `found=false`。
- 原因：先满足“值班查看最新基线健康度”诉求，同时保持 scan/update/dispatch 仍由 CLI/批处理驱动，避免扩大 Web 写权限面。
- 取舍：当前仅 API 输出，不直接改 Dashboard UI；后续再补页面摘要卡片。

## D-184 MVP 验收采用“摘要默认不拦截 + strict 可拦截”双模式
- 日期：2026-03-16
- 决策：`mvp` 命令默认将最低标准检查写入 `mvp_summary.mvp_acceptance`，不直接中断流程；新增 `--strict-acceptance` 供 CI/批处理在不达标时失败退出。
- 原因：研究阶段需要保留“即使局部不达标也能产出完整工件”能力；自动化守门场景则需要确定性的非零退出。
- 取舍：增加一个行为开关，CLI 参数略增；换来“探索与守门”两类场景同时覆盖，且不破坏既有默认体验。

## D-185 登录失败限流先采用“应用内存滑窗”实现
- 日期：2026-03-16
- 决策：Web 登录失败限流先落地为进程内 `LoginRateLimiter`（按 `ip+username`、滑动窗口计数），默认阈值 `5 次/300 秒`，超限返回 `429`。
- 原因：当前部署目标是单机 MVP，优先以最小改动降低暴力尝试风险，并保持无外部依赖。
- 取舍：多实例部署下限流计数不共享；后续如进入多副本部署，再升级到 Redis/数据库级共享限流。

## D-186 CSRF 先采用“双提交 token”表单防护
- 日期：2026-03-16
- 决策：`/login` 与 `/logout` 采用 cookie + hidden form token 的双提交校验；token 不匹配时返回 `400`。
- 原因：当前写操作均来自浏览器表单，双提交 token 在不引入额外依赖的前提下可快速降低跨站请求风险。
- 取舍：前端模板与测试需同步携带 `csrf_token`；但实现简单、易回归，符合 Web 登录 MVP 阶段目标。

## D-187 PostgreSQL 迁移先交付“占位实现 + 明确失败语义”
- 日期：2026-03-16
- 决策：在 `appdb` 新增 `Postgres*` 占位类并导出到公共接口，当前方法统一抛出 `NotImplementedError`。
- 原因：先固定迁移边界与调用契约，避免上层代码继续耦合 SQLite 细节；同时不在本轮引入新依赖与高风险改造。
- 取舍：当前无法直接切换 PostgreSQL，仅提供结构化落点；后续需要再补连接池、DDL 与数据迁移脚本。

## D-188 MVP 编排从 CLI 内联逻辑拆分为独立模块
- 日期：2026-03-16
- 决策：新增 `market_signal_system.mvp.pipeline`，由 `run_mvp_pipeline()` 统一承载 MVP 的数据接入、回测、模拟与验收；CLI `cmd_mvp` 仅负责参数解析和 strict 模式退出语义。
- 原因：`cli.py` 已承担大量命令，MVP 链路继续内联会进一步降低可读性与可测试性；拆分后可独立单测并降低后续改动风险。
- 取舍：新增一个模块与配置对象（`MvpPipelineConfig`），短期文件数增加；但职责边界更清晰，MVP 后续增强（如仓位/风控扩展）可在模块内演进而不污染 CLI。

## D-189 依赖初始化采用“单脚本三模式”
- 日期：2026-03-16
- 决策：新增 `scripts/bootstrap_project.py`，统一创建虚拟环境并支持 `pyproject`、`requirements`、`hybrid` 安装模式，README 默认推荐 `hybrid`。
- 原因：任务要求同时满足 `pyproject + requirements`，且新环境上手常见误区集中在“安装路径不一致”。单脚本可降低操作分歧。
- 取舍：初始化脚本增加维护成本；但换来更稳定的环境一致性，便于后续批处理和协作复现。

## D-190 增加离线 smoke 链路作为主链路可运行守门
- 日期：2026-03-16
- 决策：新增 `scripts/mvp_offline_smoke.py`，基于可重复合成数据覆盖 QQQ/ETH、3 策略回测与模拟交易核心字段校验。
- 原因：在线免费源存在不稳定与限流，纯依赖网络会降低“持续可运行”保障；离线 smoke 可作为最小健康检查。
- 取舍：离线结果不具备真实市场代表性，仅用于工程可运行验证；真实效果评估仍以在线数据与正式回测为准。

## D-191 离线与在线 MVP 摘要统一验收口径
- 日期：2026-03-16
- 决策：`mvp_offline_smoke` 复用 `build_mvp_acceptance`，确保离线 smoke 与在线 `mvp` 产物都输出同构 `mvp_acceptance`。
- 原因：主链路需要可在“有网/无网”两类环境下稳定守门，验收规则分叉会导致自动化结论不一致。
- 取舍：离线模式仍是合成数据，不用于收益判断；但工程验收维度可以与在线模式共享。

## D-192 数据增量更新采用“显式命令 + 隐式复用”双路径
- 日期：2026-03-16
- 决策：保留 `get_history(..., incremental=True)` 的隐式增量行为，同时新增 `DataManager.update_cache()` 与 CLI `update-cache` 命令，统一输出结构化更新摘要。
- 原因：隐式增量适合被回测/模拟链路透明复用，但运维和批处理需要显式触发、可观测的更新结果（更新行数、缓存区间变化、是否变更）。
- 取舍：新增一个命令会增加 CLI 表面积；但可显著提升日常数据维护与问题定位效率，且不破坏既有调用方兼容性。

## D-193 模拟资金曲线追加“持仓快照列”而非新增独立文件
- 日期：2026-03-16
- 决策：在既有 `sim_equity_*.csv` 直接追加 `position_*` 列（方向/数量/开仓价/盯市价/持仓浮盈），并在 `sim_summary` 中补 `latest_position`。
- 原因：中长线复盘同时需要“权益变化 + 当下持仓状态”；复用同一时序文件可减少工件分散与下游拼接成本。
- 取舍：`sim_equity` 列数增加，CSV 体积略涨；但避免再引入一个近似冗余文件，使用成本更低。

## D-194 simulate 与 backtest 统一采用 output_tag 命名扩展
- 日期：2026-03-16
- 决策：`simulate` 增加 `--output-tag`，输出工件命名策略对齐 `backtest`，用于区分同标的同策略的不同参数实验。
- 原因：批量实验场景下，默认文件名会相互覆盖，影响复现与结果追踪。
- 取舍：命名规则稍复杂；但避免覆盖风险，显著提升批处理可维护性。

## D-195 日常守门批处理优先串联“更新数据后再评估”
- 日期：2026-03-16
- 决策：新增 `refresh_mvp_daily` 模板，固定 `update-cache -> mvp -> report` 顺序，先更新本地缓存再执行评估与汇总。
- 原因：避免“评估基于旧缓存”的隐性偏差，提升 daily 结果可信度与可复现性。
- 取舍：批处理时长会增加；但换来数据新鲜度与流程一致性。

## D-192 推送与提交规范切换为“中文优先”
- 日期：2026-03-16
- 决策：从当前时点起，项目内 commit message 全部使用中文；新增/修改代码时注释优先中文；推送前必须先整理到干净状态。
- 原因：满足协作新硬约束，降低交付时的规范偏差。
- 取舍：与部分英文生态习惯不完全一致；但可通过清晰命名与测试保持工程可维护性。

## D-193 GitHub 推送采用子目录独立导出
- 日期：2026-03-16
- 决策：由于当前仓库根目录位于上级工作区，推送 `trading.git` 时采用 `market-signal-system` 子目录导出为独立仓库再推送。
- 原因：防止把工作区内其他无关文件与私有上下文一并推送到目标远程仓库。
- 取舍：会额外产生一次导出步骤；但边界更清晰，远程仓库内容可控。

## D-192 项目改为独立仓库后再执行 GitHub 推送
- 日期：2026-03-16
- 决策：先在 `market-signal-system` 目录初始化独立 Git 仓库，再执行远程绑定和推送。
- 原因：上层工作区包含大量无关与潜在敏感文件，直接推送风险高且不满足“仅推送当前项目”。
- 取舍：会丢失上层仓库与当前项目的提交关联；但换来可控、可审计、可公开发布的项目仓库边界。

## D-193 提交与注释语言约束升级为中文优先
- 日期：2026-03-16
- 决策：从本次开始，commit message 强制中文；新增/修改代码时，注释优先中文表达。
- 原因：满足协作硬约束，降低团队沟通歧义。
- 取舍：对外开源协作时可能需补英文说明；当前阶段以中文一致性优先。

## D-194 Git 协作硬约束（长期生效）
- 日期：2026-03-16
- 决策：从此仓库当前节点起，新增与后续提交必须遵守：
  1. commit message 使用中文；
  2. 新增/修改代码时优先中文注释；
  3. 推送前必须保证 `git status` clean。
- 原因：满足当前协作要求并保持审计一致性。
- 取舍：面向国际协作时可在 README 追加英文摘要，但本仓库内部记录以中文为主。
