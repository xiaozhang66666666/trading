# 2026-03-15

- 继续推进 market-signal-system，严格按 TASK.md 与小步提交执行。
- 新增组合模拟风控参数：单标的仓位上限、组合总仓位上限、回撤触发强平、冷却期阻断。
- 新增组合分配模式：`cov_risk_parity` 与 `cov_vol_target`（滚动协方差逆矩阵近似）。
- 新增组合研究告警导出：`portfolio_alerts_*.csv`（相关性+拥挤度，watch/high）。
- 新增组合风控关键路径回归测试：`test_simulate_portfolio_risk.py`。
- 新增组合权重快照导出：`portfolio_weights_*.csv`，报告索引可汇总平均总暴露。
- 持续更新 `STATUS.md` 与 `DECISIONS.md`，并完成多次里程碑提交。
- 全量测试通过：`32 passed`。
- 组合告警阈值改为可配置：`corr/crowding watch/high` 支持 `portfolio` CLI 与 `run-config` 注入。
- 新增组合模拟回归场景：总仓位耗尽阻断新开仓、连续反转交易链路；测试文件 `test_simulate_portfolio_risk.py`。
- 新增组合权重偏移导出：`portfolio_drift_*.csv`（目标权重、漂移后权重、gap、`abs_weight_gap_sum`）。
- 报告索引新增“组合权重偏移”汇总段落。
- 新增提交：
  - `4c3af2c feat: parameterize portfolio alert thresholds`
  - `c18e956 test: add portfolio allocation and reversal risk regressions`
  - `53677e8 feat: export portfolio weight drift diagnostics`
- 全量测试通过：`36 passed`。
- 新增组合漂移告警阈值：`drift_watch/high_threshold`，`portfolio_drift_*.csv` 增加 `drift_alert_level`，报告索引汇总 high/watch 次数。
- 新增示例配置：`examples/config.portfolio.strict_alerts.json`，用于批处理场景的严格告警阈值实验。
- 组合模拟新增资金占用约束：`initial_margin_rate` + `cash_reserve_ratio`，开仓受可用保证金与预留现金共同限制。
- 新增回归测试：覆盖“保证金耗尽阻断新开仓”；全量测试通过：`38 passed`。
- 报告索引新增漂移风险聚类指标：`max_high_streak` 与 `high_segments`，用于区分零散高告警与持续失衡。
- 扩展示例配置模板：新增 `config.portfolio.loose_alerts.json` 与 `config.simulate_portfolio.margin.json`，用于阈值敏感性和保证金约束批处理。
- 组合模拟新增资金快照导出：`sim_portfolio_capital_*.csv`，记录 `total_equity/gross_exposure/used_margin/available_margin/reserve_cash`。
- `run-config` 新增批处理：支持 `command: "batch"` + `tasks`（内联任务或文件引用，顺序执行）。
- 新增批处理示例：`examples/config.batch.daily.json`、`examples/config.batch.portfolio_pipeline.json`。
- `run-config` 批处理新增 `on_error` 策略：`fail_fast`（默认）/`continue`（失败汇总后抛错）。
- `report` 新增组合资金占用摘要段：汇总平均暴露率、平均/峰值保证金利用率、平均预留现金占比。
- `portfolio` 新增漂移联动再平衡实验参数：`rebalance_on_drift/rebalance_trigger/rebalance_scale`，并导出 `portfolio_rebalance_*.csv`。
- `report` 新增再平衡实验跨文件排名：按单次触发平均成本输出 best/worst 文件。
- 本轮新增提交：
  - `307a5cf feat: add batch task support for run-config`
  - `b7e33a7 feat: add portfolio capital summary to report index`
  - `a64b71c feat: add drift-triggered rebalance experiment for portfolio`
  - `bcbcf2a feat: add batch error policy and pipeline example`
  - `2a0a980 feat: rank rebalance experiments in report index`
- 全量测试持续通过：最终 `46 passed`。
- `run-config` 摘要增强：批处理默认导出 `outputs/run_config_batch_summary.json/csv`，并记录任务起止时间与 `duration_ms`。
- `run-config` 摘要新增运行级耗时：`run_started_at/run_ended_at/run_duration_ms`。
- `run-config` 配置模板新增占位符回归：缺失变量时抛出明确错误，避免模板静默误配。
- 新增批处理模板：
  - `examples/config.batch.portfolio_conservative.json`
  - `examples/config.batch.portfolio_aggressive.json`
- README 更新：补充 `vars + ${name}` 用法、continue 模式末尾抛错语义、批处理耗时字段说明。
- 状态与决策文档同步更新：新增 D-040（运行级耗时）与 D-041（保守/激进模板）。
- 全量测试通过：`52 passed`。
- 新增配置校验回归测试：覆盖 `tasks[].vars` / `tasks.file.vars` 非对象与 `summary_json/summary_csv` 非字符串输入。
- 全量测试通过：`58 passed`。
- `report` 新增批处理执行摘要段：自动识别 run-config 摘要工件并输出 run_id、成功/失败计数、总耗时与最慢任务。
- 新增回归测试：`test_build_report_index_collects_run_config_summary`。
- 全量测试通过：`59 passed`。
- 新增文档 `BATCH_PRESETS.md`：沉淀保守/激进模板的运行方法、指标口径与解读建议。
- `run-config` 新增重试参数：`retry_count` + `retry_delay_ms`，任务结果新增 `attempts` 字段。
- 新增回归测试：重试配置解析、非法值校验、失败任务重试次数落盘。
- 全量测试通过：`63 passed`。
- `report` 批处理摘要新增失败命令 TopN 统计：`failed_top_commands`。
- 回归测试扩展到 `64 passed`。
- 批处理重试策略升级：`run-config` 新增 `retry_enabled` / `retry_retryable_only`，默认仅重试可重试错误。
- 支持任务级重试覆盖：`tasks[].retry` 与 `tasks.file.retry` 可配置 `enabled/count/delay_ms/retryable_only`。
- 收紧可重试错误判定：文件缺失/权限类错误不再默认重试，避免无效重试。
- 回归测试扩展到重试开关与任务级覆盖，测试结果更新为 `67 passed`。
- `report` 新增批处理跨文件聚合统计：`aggregate_last_N_runs`（运行失败率、任务失败率、平均/P50/P90 耗时）。
- 新增回归测试 `test_build_report_index_collects_run_summary_aggregate`，锁定聚合口径。
- 全量测试更新为 `68 passed`。
- `run-config` 新增 `max_total_retries`：批处理总重试预算跨任务共享，预算耗尽后停止重试。
- 修复重试循环边界：任务级 `retry.count` 现在真正限制该任务最大尝试次数。
- 新增回归测试覆盖：任务级重试覆盖 + 总预算耗尽，测试总数更新为 `73 passed`。
- 最小修复：清理 `DECISIONS.md` 重复 `D-049`，恢复文档编号唯一性；主链路命令与全量测试复验通过。
- 新增 `scripts/compare_batch_presets.py`，打通 README 中“保守/激进一键对比”实际可执行入口。
- 新增脚本冒烟测试 `test_compare_batch_presets_script_help`，防止入口再次失效。
- 全量测试更新为 `75 passed`。
- 新增报告聚合增强：`aggregate_all_runs` + `aggregate_last_N_runs` 双口径（全量 + 最近窗口）。
- `run-config` 新增总重试预算 `max_total_retries`，摘要新增 `retry_budget_remaining` 与任务级 `retries_used`。
- 新增一键对比脚本：`scripts/compare_batch_presets.py`，产出 `outputs/preset_compare/preset_compare.csv/.md`。
- 新增回归：`tests/test_preset_compare.py` 与 `tests/test_compare_batch_presets_script.py`。
- 本轮全量测试：`75 passed`。
- 本轮提交：`784118d`、`a122113`、`44a700a`、`95f298e`。
- `report` 批处理聚合新增重试效率统计：`retry_hit_rate`、`total_retries_used`、`non_retryable_fail_share`。
- 新增回归测试覆盖重试效率口径，防止报告指标回归。
- 全量测试更新为 `76 passed`。
- 主链路最小修复：`preset_compare` 在批处理子进程非零退出时立即抛错，避免假成功。
- 回归修复：`test_compare_presets_applies_vars_override_when_running_batch` 补充 `Path` 导入，恢复全量测试。
- 全量测试通过：`82 passed`。
- 主链路最小修复：`preset_compare` 在批处理子进程非零退出时立即抛错，避免假成功。
- 回归修复：`test_compare_presets_applies_vars_override_when_running_batch` 补充 `Path` 导入，恢复全量测试。
- 全量测试通过：`82 passed`。
- 数据层增强：新增 `BinanceSpotProvider`，ETH 改为 `Binance ETHUSDT` 主源 + `Yahoo ETH-USD` 回退。
- 新增数据层回归：`test_fetch_with_fallback_uses_next_provider`、`test_binance_provider_parses_klines`。
- 报告增强：新增“重试预算轨迹”摘要段（文件级 + aggregate）。
- 新增脚本回归：`test_compare_batch_presets_script_accepts_start_end_pair`，覆盖 `--start/--end` 成功路径。
- 文档同步：更新 `README.md`（ETH 双源描述）、`STATUS.md`、`DECISIONS.md`（D-054/D-055）。
- 本轮新增提交：
  - `414fcc2 feat: add binance primary provider with yahoo fallback for eth`
  - `5e61f69 feat: extend report with retry budget trace summary`
- 当前全量测试：`87 passed`。
- 新增可选真实网络集成测试：`tests/test_data_provider_integration.py`，默认跳过；`RUN_BINANCE_INTEGRATION=1` 才执行 Binance 联通性验证，并对 `HTTP 451` 自动 `skip`。
- 报告增强：`retry_budget_trace` 新增跨 run 趋势口径 `aggregate_last_N_runs`，输出 `depleted_run_rate/avg_depleted_rate/avg_failed_ratio`。
- 预设对比新增快照保留策略：`compare_presets(..., keep_snapshot_count=20)` 与脚本参数 `--keep-snapshots`（`0` 不清理）。
- 文档更新：`README.md`、`BATCH_PRESETS.md` 补充快照保留参数说明。
- 新增回归测试：
  - `test_build_report_index_collects_retry_budget_trend_last_runs`
  - `test_compare_presets_cleans_old_snapshots`
  - `test_compare_batch_presets_script_rejects_negative_keep_snapshots`
- 本轮新增提交：
  - `849e86e test: add optional live binance integration coverage`
  - `082559b feat: add retry budget trend aggregate for recent runs`
  - `786b53a feat: add retention policy for preset compare snapshots`
- 最新全量测试：`93 passed, 1 skipped`。
- 新增 `run-config` 压缩命名策略落地：`compress_naming=auto_suffix/strict`，统一作用于 `summary_*` 与 `retry_budget_trace_*` 的 gzip 输出路径处理。
- 增加 `compress_naming` 回归覆盖：配置解析默认/非法值、strict 模式下缺少 `.gz` 后缀时报错。
- 报告增强：`cleanup.latest` 摘要新增 `generated_at`，显示最近一次快照清理执行时间。
- 本轮新增提交：
  - `f052aca feat: add strict compress naming policy for run-config outputs`
- 当前全量测试：`114 passed, 1 skipped`。
- 预设清理链路增强：`compare_batch_presets.py` 对非法日期参数改为 CLI 友好报错（`parser.error`），避免 Python 堆栈暴露。
- 新增脚本回归：`test_compare_batch_presets_script_rejects_invalid_snapshot_date`。
- 报告增强：`cleanup.latest` 新增 `generated_at`，显示最近一次快照清理执行时间。
- 严格命名策略补齐正向回归：`test_run_config_strict_compress_naming_accepts_gz_suffix`。
- 新增 strict 模板：
  - `examples/config.batch.strict_compress.json`
  - `examples/config.batch.daily.strict_gzip.json`
- 本轮新增提交：
  - `d3fcb94 fix: show preset cleanup validation errors via parser`
  - `82002f9 test: cover invalid snapshot date handling in script`
  - `0161293 feat: include cleanup generated_at in report summary`
  - `74ea8d8 feat: add strict gzip config templates and validation test`
- 最新全量测试：`115 passed, 1 skipped`。
- 新增严格压缩命名模板：`examples/config.batch.daily.strict_gzip.json`（`summary_compress=gzip` + `compress_naming=strict` + 显式 `.gz` 路径）。
- 新增回归测试：`test_run_config_strict_compress_naming_accepts_gz_suffix`，覆盖 strict 模式成功写入 `.json.gz/.csv.gz`。
- 修正 `STATUS.md` 中严格模板路径，避免文档指向不存在文件。
- 本轮全量测试更新：`115 passed, 1 skipped`。
- 新增端到端模板：`examples/config.batch.daily.strict_gzip_trace.json`（strict 命名 + 摘要/预算轨迹双 gzip 工件）。
- 新增 strict 预算轨迹后缀校验回归：
  - `test_run_config_strict_compress_naming_requires_gz_suffix_for_retry_trace`
  - `test_run_config_strict_compress_naming_accepts_gz_suffix_for_retry_trace`
- 全量测试更新：`117 passed, 1 skipped`。
- 新增 `BATCH_PRESETS.md` strict 失败演示复盘清单（运行命令 + 工件核对 checklist）。
- 报告层 cleanup 历史聚合增强容错：非法数字安全降级为 0，非法时间记录跳过，不再中断报告生成。
- 新增回归测试 `test_build_report_index_tolerates_invalid_preset_cleanup_history_rows`。
- 本轮验证通过：`pytest -q tests/test_reporting.py tests/test_demo_strict_failure_report_script.py` 与全量 `124 passed, 1 skipped`。
- cleanup 历史趋势摘要新增异常计数：`skipped_invalid_time` 与 `rows_with_invalid_numeric`。
- 新增/更新回归测试后全量测试仍通过：`124 passed, 1 skipped`。
- `demo_strict_failure_report.py` 回归增强为内容级校验：断言 summary/report 工件存在，且报告包含“批处理执行摘要”。
- README 报告说明已补齐 cleanup 聚合异常计数字段：`skipped_invalid_time` 与 `rows_with_invalid_numeric`。
- cleanup 历史趋势摘要新增 `history_files`、`valid_runs`、`abnormal_ratio`，用于衡量异常密度。
- 新增中长线策略 `macd_regime`：MACD 线/信号线确认 + `MACD/价格` 阈值 + regime/波动率过滤；已接入注册表与 CLI。
- 策略回归新增 `test_macd_regime_signal_shape`，验证 `signal/explain` 契约一致。
- 新增模拟交易 CLI 恢复回归 `test_cmd_simulate_can_resume_from_persisted_state`：两段运行共享同一 `state_file`，验证重启后状态可延续并完成平仓。
- 本轮测试结果：`pytest -q` => `126 passed, 1 skipped`。
- 新增模板 `examples/config.research.macd_regime.json`，提供 QQQ 上的 macd_regime 默认参数网格研究入口。
- 更新 `examples/config.compare.json` 默认策略列表，加入 `macd_regime` 保持与 CLI/注册表一致。
- README `run-config` 示例新增 macd_regime 研究模板命令。
- 相关验证：`pytest -q tests/test_config_runner.py` => `48 passed`。
- 新增单策略复现模板：`examples/config.backtest.macd_regime.json`，并将命令接入 README 的 `run-config` 示例清单。
- 新增 CLI 子进程恢复冒烟：`test_simulate_cli_can_resume_state_via_subprocess_without_network`（本地缓存离线数据 + 两段 simulate 复用同一 state_file）。
- 新增 `backtest --output-tag`：同策略多参数批处理可并存输出工件，避免 `metrics/equity/trades/signals` 被覆盖。
- 新增参数敏感性模板：`examples/config.batch.macd_sensitivity.json`（三组 macd_regime 参数 + report 串行）。
- 新增 `output_tag` 回归：
  - `test_parse_config_task_backtest_accepts_output_tag`
  - `test_backtest_cli_output_tag_avoids_overwrite_via_subprocess`
- 新增模拟恢复模板：`examples/config.batch.simulate_resume.json`（两段 simulate + 共享 state_file + report）。
- 新增配置解析回归：`test_parse_config_tasks_supports_simulate_resume_pipeline`。
- 本轮测试：
  - `pytest -q tests/test_config_runner.py tests/test_cli_entry.py` => `52 passed`
  - `pytest -q tests/test_config_runner.py` => `50 passed`
- 本轮新增提交：
  - `0352d0e feat: add macd_regime backtest template for run-config`
  - `e0e6d6b test: add subprocess simulate resume smoke coverage`
  - `6ca9b43 feat: support backtest output tags for sensitivity batches`
  - `afd2277 feat: add run-config simulate resume pipeline template`
- 新增模板：`examples/config.batch.output_tag_compare.json`，统一 `output_tag` 命名示例（`symbol+strategy+关键参数`）并自动串接 report。
- 新增脚本：`scripts/smoke_simulate_resume.py`，离线构造缓存后连续执行两段 `simulate`，自动校验 `second_trade_count > first_trade_count`。
- 新增脚本回归：`tests/test_smoke_simulate_resume_script.py`（help/默认成功/自定义 split 成功/非法时间顺序失败）。
- 新增模板回归：`test_parse_config_tasks_supports_output_tag_compare_template`，锁定 output_tag 对照模板结构。
- `smoke_simulate_resume.py` 增加日期顺序校验：`start < split-end < end`，非法输入走 CLI 友好报错。
- 本轮新增提交：
  - `064435b docs: add output_tag batch template and naming guidance`
  - `b5a036e feat: add offline smoke script for simulate resume flow`
  - `c2ceee5 test: guard output_tag compare template contract`
  - `3d6e155 fix: validate date windows in simulate resume smoke script`
- 最新全量测试：`135 passed, 1 skipped`。
- `run_score_vs_macd.py` 新增 `--report-prefix`：支持报告文件名前缀统一命名，可与 `--report-file/--report-tag/--report-dir` 叠加。
- 新增脚本回归：覆盖 `--report-prefix` 独立、与 `--report-tag` 叠加、空前缀报错。
- `run_score_vs_macd.py` 新增 `--emit-config-dir`：按目录自动落盘 `score_vs_macd_<symbol>_{ts}.json`，并兼容 `--keep-emitted-configs` 历史清理。
- 新增脚本回归：覆盖 `--emit-config-dir` 输出路径契约、与 `--emit-config` 互斥、与 `--keep-temp-config` 互斥、历史清理路径。
- README/STATUS/DECISIONS 同步更新，新增 D-117（report-prefix）与 D-118（emit-config-dir）。
- 本轮新增提交：
  - `984afd3 feat: add report prefix option for score_vs_macd script`
  - `e71a512 feat: add emit-config-dir shortcut for score_vs_macd script`
- 本轮全量测试：`175 passed, 1 skipped`。
- 新增中长线策略 `score_regime`：三因子加权评分（动量 + 趋势偏离 + 均值回归）叠加波动率过滤，支持多空/空仓。
- 策略接入同步完成：`get_strategy` 注册、CLI 策略枚举、`compare` 默认策略列表与 `examples/config.compare.json`。
- 新增回归测试 `test_score_regime_signal_shape`，验证 `signal/explain/reason` 契约一致。
- 本轮验证：`pytest -q tests/test_strategies.py tests/test_compare.py tests/test_config_runner.py` => `58 passed`。
- 本轮新增提交：`8662359 feat: add score_regime weighted multi-factor strategy`。
- 新增模板：`examples/config.research.score_regime.json`，提供 `score_regime` 默认参数网格研究入口。
- 新增配置解析回归：`test_parse_config_task_supports_score_regime_research_template`，锁定 `run-config` 对新模板的解析契约。
- 新增 CLI 子进程离线回归：`test_compare_cli_default_strategies_runs_via_subprocess_without_network`，覆盖默认策略集（含 `score_regime`）在无 `PYTHONPATH` 场景下可执行。
- 新增命名约定模板：`examples/config.batch.score_vs_macd_naming_demo.json`，统一 `output_tag/report` 的 `batch_prefix + symbol + strategy + batch_tag` 规则。
- `run_score_vs_macd.py` 新增 `--emit-separator`（默认 `_`，可改为 `-` 等），支持目录级渲染配置命名风格统一。
- 新增脚本回归：覆盖 `--emit-separator` 成功路径（含与 `emit-prefix/tag` 叠加）与误用校验（未搭配目录、空白、路径分隔符）。
- 本轮验证：`pytest -q tests/test_run_score_vs_macd_script.py` => `40 passed`。
- 本轮新增提交：
  - `8eb77c9 docs: add score_vs_macd naming demo batch template`
  - `9c3eadf feat: add configurable emit separator for score_vs_macd`
- 新增 ETH 命名演示模板：`examples/config.batch.score_vs_macd_naming_demo.eth.json`，参数口径与 ETH 专项模板对齐。
- README/STATUS/DECISIONS 同步补充 ETH 命名模板入口与决策记录（D-125）。
- 本轮新增提交：`c6f46d4 docs: add eth variant for score_vs_macd naming demo`。
- `run_score_vs_macd.py` 新增 `--emit-template`：目录级渲染配置支持自定义文件名模板（强制 `{symbol}` + `{ts}`）。
- 参数约束：`--emit-template` 仅可与 `--emit-config-dir` 搭配，且与 `--emit-prefix/--emit-tag/--emit-separator` 互斥。
- 新增脚本回归：覆盖 `--emit-template` 成功路径、互斥校验、占位符缺失报错。
- 本轮验证：`pytest -q tests/test_run_score_vs_macd_script.py` => `44 passed`。
- 本轮新增提交：`48154e2 feat: support emit template for score_vs_macd config naming`。
- 本轮定向验证：`pytest -q tests/test_config_runner.py tests/test_cli_entry.py -k "score_regime or compare_cli_default_strategies_runs_via_subprocess_without_network"` => `2 passed`。
- 新增模板：`examples/config.backtest.score_regime.json`，提供 `score_regime` 固定参数单策略回测复现入口。
- 新增配置解析回归：`test_parse_config_task_supports_score_regime_backtest_template`，锁定模板字段与参数解析。
- README 的 `run-config` 示例清单已补齐 `config.backtest.score_regime.json`。
- 本轮验证：`pytest -q tests/test_config_runner.py -k score_regime` => `2 passed`。
- `run_score_vs_macd.py` 命名校验与拼接逻辑下沉到 `utils/emit_naming.py`，减少脚本重复代码并补充 `test_emit_naming.py`。
- 新增 `run_score_vs_macd.py --naming-demo`：切换命名演示模板并默认只渲染不执行，新增脚本回归覆盖 QQQ/ETH 路径。
- 新增 `run_score_vs_macd.py --run`：显式覆盖 `--naming-demo` 默认 no-run，并新增与 `--no-run/--dry-run` 互斥回归。
- 文档与状态同步：`README.md` 增补 naming-demo/run 示例；`STATUS.md`、`DECISIONS.md` 新增 D-130/D-131/D-132。
- 本轮新增提交：
  - `af5c3f5 refactor: extract emit naming helpers for score_vs_macd script`
  - `19731af feat: add naming-demo quick mode for score_vs_macd script`
  - `2ffabe4 feat: add explicit --run override for naming-demo mode`
- 本轮定向测试：`pytest -q tests/test_run_score_vs_macd_script.py tests/test_emit_naming.py` => `52 passed`。
- 新增中长线策略 `atr_regime`：ATR 归一化趋势强度 + 波动率分位过滤，支持多空/空仓并完成注册表接入。
- CLI 策略清单与默认对比集合同步扩展：`compare` 默认策略和 `examples/config.compare.json` 均加入 `atr_regime`。
- 新增模板：
  - `examples/config.backtest.atr_regime.json`
  - `examples/config.research.atr_regime.json`
- 新增回归：
  - `test_atr_regime_signal_shape`
  - `test_parse_config_task_supports_atr_regime_research_template`
  - `test_parse_config_task_supports_atr_regime_backtest_template`
  - `test_compare_cli_default_strategies_runs_via_subprocess_without_network` 增补 `atr_regime` 断言
- 文档同步：`README.md`、`STRATEGIES.md`、`STATUS.md`、`DECISIONS.md`（D-127）。
- 本轮验证：
  - 定向：`8 passed`
  - 全量：`197 passed, 1 skipped`
- 数据层新增 `CoinGeckoProvider`（`market_chart/range` 重采样为 `OHLCV`），支持 `1d/1h/4h`。
- ETH 路由升级为三级回退：`Binance ETHUSDT -> Yahoo ETH-USD -> CoinGecko ethereum`。
- 新增回归：`test_coingecko_provider_builds_ohlcv_from_market_chart`。
- 本轮验证：
  - 定向：`4 passed, 1 skipped`
  - 全量：`198 passed, 1 skipped`
- 新增模板：`examples/config.batch.atr_regime_stability.json`，串联 `research -> stability -> report`。
- 新增回归：`test_parse_config_tasks_supports_atr_regime_stability_batch_template`。
- README 已补充该模板命令入口。
- 本轮验证：
  - 定向：`3 passed`
  - 全量：`199 passed, 1 skipped`
- 本轮新增提交：`20c2cd1 feat: add score_regime backtest config template`。
- 新增批处理模板：`examples/config.batch.score_regime_stability.json`，串联 `research -> stability -> report`。
- 新增配置解析回归：`test_parse_config_tasks_supports_score_regime_stability_batch_template`，覆盖任务顺序与占位符渲染。
- README 已补充该模板的一键运行命令。
- 本轮验证：`pytest -q tests/test_config_runner.py -k "score_regime_stability_batch_template or score_regime"` => `3 passed`。
- 本轮新增提交：`dbaa364 feat: add score_regime stability pipeline batch template`。
- 新增数据质量诊断链路：`diagnose-data` CLI + `diagnose_ohlcv_quality`，可输出 `data_quality_*.json` 与 `data_quality_anomalies_*.csv`。
- 新增模板：`examples/config.diagnose.qqq.json`；新增离线 CLI 回归：`test_diagnose_data_cli_runs_via_subprocess_without_network`。
- `simulate` 新增动态仓位参数：`allocation_per_signal` / `min_quantity`，支持按权益比例开仓并阻断过小订单。
- 新增模板：`examples/config.simulate.allocation.json`；新增回归：`test_cmd_simulate_supports_allocation_per_signal_dynamic_quantity`。
- `simulate` 新增逐 bar 资金曲线导出：`sim_equity_<symbol>_<strategy>.csv`。
- `report` 新增“单标的模拟资金曲线摘要”，自动统计起止权益、总收益、最大回撤。
- 本轮新增提交：
  - `cb2379d feat: add data quality diagnostics command and reports`
  - `b6f13a7 feat: support equity-based sizing in simulate command`
  - `01f28c9 feat: export bar-level equity curve for simulate runs`
  - `4f54c4d feat: summarize simulate equity curves in report index`
- 最新全量测试：`147 passed, 1 skipped`。
- 新增 `score_vs_macd` 专项模板：`examples/config.batch.score_vs_macd_compare.json`（QQQ）与 `examples/config.batch.score_vs_macd_compare.eth.json`（ETH）。
- 新增模板解析回归：`test_parse_config_tasks_supports_score_vs_macd_compare_batch_template` 与 ETH 版本，锁定 `backtest/backtest/compare/report` 任务结构。
- `STRATEGIES.md` 补齐 `score_regime` 与 `macd_regime` 参数建议区间，并与专项模板默认参数对齐。
- 新增一键脚本 `scripts/run_score_vs_macd.py`：支持 `--symbol qqq|eth` 与 `--dry-run`，可直接触发对应 `run-config` 模板。
- `report` 新增“策略对照摘要（score_regime vs macd_regime）”：同标的工件齐全时自动输出 `score_minus_macd` 关键差值。
- 本轮新增提交：
  - `addf511 feat: add score_vs_macd batch comparison template`
  - `46ea2d0 docs: align strategy parameter ranges with comparison template`
  - `c6659d1 feat: add eth score_vs_macd batch template`
  - `e9a6810 feat: add one-click runner for score_vs_macd templates`
  - `94629ce feat: add score_vs_macd summary section to report`
- 本轮定向回归通过：
  - `pytest -q tests/test_config_runner.py -k "score_vs_macd..."`
  - `pytest -q tests/test_run_score_vs_macd_script.py`
  - `pytest -q tests/test_reporting.py -k report_index_collects_score_vs_macd_summary`
- 新增 `run_score_vs_macd.py` 时间窗覆盖：`--start/--end`，并校验成对提供与 `start < end`。
- 新增 `run_score_vs_macd.py --no-run`：可渲染临时配置并仅打印执行命令。
- 新增 `run_score_vs_macd.py --report-file`：运行时覆盖 batch 中 report 输出文件名。
- 新增 `run_score_vs_macd.py --print-config`：直接输出渲染后的 JSON 配置用于巡检。
- 新增 `run_score_vs_macd.py --keep-temp-config`：真实执行路径可保留临时配置用于审计。
- 补齐脚本回归测试（help/dry-run/no-run/参数校验/失败路径/保留临时配置），`tests/test_run_score_vs_macd_script.py` 更新到 8 用例。
- README、STATUS、DECISIONS 同步更新（新增 D-107~D-111）。
- 全量测试复验通过：`158 passed, 1 skipped`。
- 本轮新增提交：`5a600d7`、`7251fb7`、`3844c46`、`25e8f5d`、`a49c9ea`。
- `run_score_vs_macd.py` 新增 `--report-tag`：支持运行时给 report 文件追加标签，未传值时自动使用 UTC 时间戳后缀。
- `run_score_vs_macd.py` 新增 `--emit-config`：渲染配置可写入指定路径并复用执行，提升审计与复盘可追溯性。
- 新增脚本回归测试：覆盖 `--report-tag`（显式/自动）与 `--emit-config` 输出契约。
- README/STATUS/DECISIONS 已同步更新；下一步聚焦 `--report-dir` 与 emitted 配置清理策略。
- `run_score_vs_macd.py` 新增 `--report-dir`：仅覆盖报告目录，保留文件名，且可与 `--report-tag` 叠加。
- 新增脚本回归测试：覆盖 `--report-dir` 单独使用与 `--report-dir + --report-tag` 组合输出。
- `run_score_vs_macd.py` 增加参数冲突前置校验：拒绝 `--no-run + --dry-run`、`--keep-temp-config + --emit-config` 及 `--keep-temp-config + --no-run/--dry-run`。
- 新增脚本回归测试，锁定参数冲突场景的返回码与错误文案。
- `run_score_vs_macd.py` 的 `--emit-config` 支持 `{ts}` 时间戳占位符，新增 `--keep-emitted-configs` 自动保留最近 N 份并清理历史。
- emitted 配置清理策略采用“移入 `.trash/score_vs_macd_emitted/`”而非直接删除，保证可恢复性。
- `run_score_vs_macd.py` 新增 `--emit-prefix`：仅在 `--emit-config-dir` 模式下给自动配置文件名追加前缀。
- `run_score_vs_macd.py` 新增 `--emit-tag`：仅在 `--emit-config-dir` 模式下给自动配置文件名追加后缀批次标识。
- 新增脚本回归测试：覆盖 `emit-prefix/emit-tag` 生效路径、两者叠加命名、空值校验与误用参数校验。
- 本轮定向回归：`pytest -q tests/test_run_score_vs_macd_script.py` 通过（先 28 passed，后 32 passed）。
- `run_score_vs_macd.py` 命名增强联动验证：`emit-prefix + emit-tag` 与 `keep-emitted-configs` 可共同工作，旧文件清理逻辑保持有效。
- README 已补“同目录多专题多批次 + 历史保留窗口”命令示例。
- 本轮定向回归：`pytest -q tests/test_run_score_vs_macd_script.py` => `33 passed`。
- `run_score_vs_macd.py` 新增命名参数字符集约束：`--emit-prefix/--emit-tag` 禁止包含 `/` 或 `\\`，避免路径分隔符误用。
- 新增回归测试：覆盖 prefix/tag 含路径分隔符时的 CLI 友好报错。
- 本轮定向回归：`pytest -q tests/test_run_score_vs_macd_script.py` => `35 passed`。
- 回测指标增强：`BacktestEngine` 默认输出买入持有基准对比字段（`benchmark_total_return/excess_total_return/information_ratio/active_max_drawdown`），并接入 `report` 单策略摘要展示。
- 新增回归覆盖：`test_summarize_metrics_with_benchmark_fields`、`test_backtest_includes_benchmark_comparison_metrics`、`test_build_report_index_collects_metrics`（基准字段断言）。
- 排行榜评分口径升级：`compare` 新增 `score_excess_return`，综合评分权重调整为 `0.30/0.20/0.20/0.20/0.10`（Sharpe/Calmar/Total/Excess/Drawdown）。
- 新增回归：`test_build_leaderboard_has_composite_score` 断言 `score_excess_return` 与 `excess_total_return` 字段存在。
- README/STATUS/DECISIONS 已同步，新增决策：`D-133`（单策略基准对比）、`D-134`（排行榜纳入超额收益）。
- 本轮提交：
  - `a003e8d feat: add buy-and-hold benchmark metrics for backtest`
  - `ed034fe feat: include excess return in compare leaderboard scoring`
- 本轮验证：`pytest -q` => `209 passed, 1 skipped`。
- 组合回测基准增强：`run_portfolio_backtest_detailed` 默认计算同窗等权持有基准并写入统一字段（`benchmark_total_return/excess_total_return/information_ratio`）。
- 报告增强：`report` 的“组合回测”段落在工件包含基准字段时同步展示基准总收益、超额收益与信息比率。
- 新增回归：`test_equal_weight_portfolio_outputs_metrics_and_positions` 断言组合 metrics 包含基准字段；`test_build_report_index_collects_portfolio_metrics_with_benchmark` 锁定报告展示契约。
- 新增决策：`D-135`（组合基准统一为同窗等权持有）。
- 本轮新增提交：`663ccc4 feat: add benchmark comparison metrics for portfolio backtests`。
- 最新全量测试：`210 passed, 1 skipped`。
- 新增 `compare` 分组排行榜导出：`leaderboard_by_symbol_<symbols>.csv`，含 `symbol_rank/symbol_strategy_count`，并在 CLI 同步落盘。
- 新增回归：`test_build_symbol_leaderboard_adds_group_rank` 与 `test_compare_cli_default_strategies_runs_via_subprocess_without_network`（断言分组榜工件存在）。
- 报告增强：`多策略排行榜` 段新增 `excess_top/excess_bottom`（文件级）和 `aggregate_excess_top/bottom`（跨文件）。
- 报告增强：新增 `分组排行榜（按标的）` 段，从 `leaderboard_by_symbol_*.csv` 自动输出每个 symbol 的冠军策略。
- 新增回归：`test_build_report_index_collects_leaderboard_excess_top_bottom`、`test_build_report_index_collects_symbol_group_champions`。
- 文档与决策同步：README/STATUS/DECISIONS 新增 D-136/D-137/D-138。
- 本轮提交：
  - `a3db479 feat: add symbol-grouped leaderboard output`
  - `da04b1c feat: summarize excess return top and bottom in report`
  - `cc7371b feat: add symbol champion summary to report index`
- 定向测试通过：
  - `pytest -q tests/test_compare.py tests/test_cli_entry.py -k compare` -> `3 passed`
  - `pytest -q tests/test_reporting.py -k "leaderboard_excess_top_bottom or symbol_group_champions"` -> `2 passed`
