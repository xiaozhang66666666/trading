# 2026-03-16

- 按 STATUS 下一步推进并完成 3 个里程碑：
  - `run-config` 批处理摘要接入 compare 分组冠军快照（`symbol_group_champions` + 任务行字段）。
  - `report` 批处理摘要接入冠军快照展示（`compare_task#N champions=...`）。
  - `report` 新增分组冠军稳定性（最近 10 份分组榜单）与批处理冠军切换趋势（`champion_switch_last_N_runs`）。
- 新增回归测试：
  - `test_run_config_summary_includes_symbol_group_champions_for_compare_task`
  - `test_build_report_index_collects_run_config_symbol_group_champions`
  - `test_build_report_index_collects_symbol_group_champion_stability`
  - `test_build_report_index_collects_run_config_champion_switch_trend`
- 本轮提交：
  - `100cac5 feat: add symbol champion snapshots to run-config summary`
  - `a1c39a8 feat: add symbol champion stability section in report`
  - `89e8b2e feat: add champion switch trend in run summary report`
- 定向测试通过，未执行全量 `pytest -q`。
- 分组冠军稳定性与批处理冠军趋势新增告警分级：`alert=none/watch/high`（阈值可配，默认 `watch=1/high=2`）。
- `report` 新增冠军切换趋势 CSV 导出：默认输出 `outputs/champion_switch_last_runs.csv`（`symbol/samples/switches/unique_champions/latest/alert_level`）。
- `report` CLI 新增参数：
  - `--champion-switch-csv-file`
  - `--champion-switch-recent-runs`
  - `--champion-switch-watch-threshold`
  - `--champion-switch-high-threshold`
- 新增回归测试：
  - `test_build_report_index_champion_switch_alert_threshold_override`
  - `test_export_champion_switch_trend_csv`
  - `test_parse_config_task_report_defaults`（新增 report 参数默认值断言）
- 本轮定向验证：
  - `pytest -q tests/test_reporting.py tests/test_config_runner.py` => `98 passed`
  - `pytest -q tests/test_cli_entry.py -k module_entry_works_without_pythonpath` => `1 passed`
- 新增 `mvp` CLI：一键串联 `QQQ+ETH` 数据拉取、3 策略回测、单标的模拟交易，并输出 `outputs/mvp_summary_<run_tag>.json`。
- 新增模板：`examples/config.mvp.json`，支持通过 `run-config` 复现 MVP 基线。
- 新增回归测试：
  - `test_parse_config_task_mvp_defaults`
  - `test_mvp_cli_runs_end_to_end_via_subprocess_without_network`
- 本轮验证：
  - `pytest -q tests/test_config_runner.py -k mvp_defaults` => `1 passed`
  - `pytest -q tests/test_cli_entry.py -k mvp_cli_runs_end_to_end_via_subprocess_without_network` => `1 passed`
- 新增脚本：`scripts/mvp_report.py`，可从 `mvp_summary_*.json` 生成按标的分组策略榜单（CSV）与简报（Markdown）。
- 新增脚本回归：
  - `test_mvp_report_script_help`
  - `test_mvp_report_script_builds_outputs_from_summary`
- 新主任务 `TASK_SIGNAL_PILOT.md` 已推进完成 Signal Pilot MVP：
  - 台账持久化：`data/state/signal_alert_ledger.csv`
  - 新信号扫描：`scan-alerts`（同向 open 去重 + JSONL 待通知队列）
  - 观察收益刷新：`update-alerts`（current/MFE/MAE/holding + reverse/expired）
  - 观察报告：`report-alerts`（7/30/60 天，CSV/JSON/Markdown）
- 本轮新增测试：
  - `test_signal_pilot_ledger.py`
  - `test_signal_pilot_scan.py`
  - `test_signal_pilot_update.py`
  - `test_signal_pilot_report.py`
  - `test_parse_config_task_supports_signal_pilot_commands`
- 本轮提交：
  - `8905f85 feat: add persistent alert ledger for signal pilot`
  - `6919637 feat: add scan-alerts workflow with dedup and notification queue`
  - `2f9a6dc feat: add update-alerts workflow for signal performance tracking`
  - `add9a69 feat: add report-alerts command with 7-30-60 performance summary`
  - `cdb5a5b docs: document signal pilot mvp workflow and decisions`
- 新增批处理模板：`examples/config.batch.mvp_daily.json`，默认串联 `mvp -> report`。
- 新增配置解析回归：`test_parse_config_tasks_supports_mvp_daily_batch_template`。
- 新增 `baseline-summary` 命令：聚合最新 `mvp_summary + signal_pilot_report + report_index`，输出去重摘要 `outputs/baseline_daily_digest.json/csv`。
- `examples/config.batch.baseline.daily.json` 已扩展为 6 任务链路：`mvp -> scan-alerts -> update-alerts -> report-alerts -> report -> baseline-summary`。
- 新增回归：
  - `tests/test_baseline_summary.py::test_build_baseline_daily_summary_collects_mvp_signal_and_report`
  - `tests/test_cli_entry.py::test_baseline_summary_cli_runs_via_subprocess_without_network`
  - `tests/test_config_runner.py::test_parse_config_task_baseline_summary_defaults`
- 定向测试通过：`pytest -q tests/test_baseline_summary.py tests/test_config_runner.py tests/test_cli_entry.py -k "baseline_summary or baseline_daily_batch_template or baseline_summary_defaults"` => `4 passed`。
- 新增 Signal Pilot 通知派发命令 `dispatch-alerts`：默认本地 sink（`signal_pilot_notifications_dispatched.jsonl`），可选 webhook；成功事件回写 ledger `notification_sent=true`。
- 新增模板：`examples/config.signal_pilot.dispatch.json`；批处理模板升级为 `scan -> dispatch -> update -> report`（同时更新 baseline daily 模板）。
- 新增回归：
  - `tests/test_signal_pilot_notify.py`（本地 sink + dry-run）
  - `tests/test_cli_entry.py::test_dispatch_alerts_cli_runs_via_subprocess_without_network`
  - `tests/test_config_runner.py` 扩展 dispatch/default/template 覆盖
- 本轮验证：`pytest -q tests/test_signal_pilot_ledger.py tests/test_signal_pilot_scan.py tests/test_signal_pilot_update.py tests/test_signal_pilot_report.py tests/test_signal_pilot_notify.py tests/test_config_runner.py tests/test_cli_entry.py tests/test_baseline_summary.py` => `89 passed`。
- 模板去硬编码日期：`config.mvp.json` 与 `config.signal_pilot.scan/update/report.json` 改为 `vars(start/end/as_of)` 占位符。
- `config.batch.signal_pilot.daily.json` 与 `config.batch.baseline.daily.json` 升级为任务级 `vars` 透传，支持统一日期覆盖。
- 新增自动化脚本 `scripts/run_baseline_daily.py`：支持 `--start/--end/--as-of` 渲染基线模板并执行，支持 `--no-run` 仅渲染。
- 新增脚本回归：`tests/test_run_baseline_daily_script.py`（help + 日期覆盖渲染）。
- `run-config` 增加最小 schema 校验：已支持命令前置拦截未知字段、缺失必填、类型不匹配、非法 choices；保留批处理中未知命令的失败统计/重试语义。
- 新增配置校验回归：
  - `test_parse_config_task_rejects_unknown_field`
  - `test_parse_config_task_rejects_missing_required_field`
  - `test_parse_config_task_rejects_invalid_value_type`
  - `test_parse_config_task_rejects_invalid_choice`
- 新增 YAML 模板：`examples/config.batch.signal_pilot.daily.yaml` 与 `examples/config.batch.baseline.daily.yaml`，并补齐解析回归。
- `run-config` schema 新增常见数值边界校验（如 `fee_rate>=0`、`allocation_per_signal∈(0,1]`、`champion_switch_recent_runs>0`）。
- 新增数值边界回归：
  - `test_parse_config_task_rejects_negative_fee_rate`
  - `test_parse_config_task_rejects_invalid_allocation_per_signal`
  - `test_parse_config_task_rejects_non_positive_recent_runs`
- 本轮新增提交：
  - `bdecb57 feat: add minimal schema validation for run-config`
  - `a6c8f0b feat: add yaml batch templates for baseline and signal pilot`
  - `15a8184 feat: enforce basic numeric bounds in run-config schema`
- Signal Pilot `dispatch-alerts` 增强：
  - 新增 webhook 失败重试参数：`retry_count`、`retry_delay_ms`（仅重试可重试错误）。
  - 新增幂等窗口：`idempotency_window_minutes`（窗口内跳过同一 `alert_id`）。
  - ledger 新增持久化字段：`notification_last_attempt_at`、`notification_last_sent_at`、`notification_fail_count`。
  - 新增回归：`test_dispatch_alerts_webhook_retry_then_success`、`test_dispatch_alerts_idempotency_window_skips_recent_attempt`、`test_parse_config_task_rejects_negative_dispatch_retry_count`。
  - 本轮提交：`676004a feat: add retry and idempotency window for signal dispatch`、`7819af5 test: cover dispatch retry and idempotency config defaults`。
- 主链路最小修复：`dispatch-alerts` 在队列文件不存在时，摘要返回字段与正常路径对齐（补齐 `sink_file/channel/retry_*`），避免上游聚合读取 KeyError。
- baseline-summary 增强：新增 `signal_dispatch` 指标聚合（`open/pending/failed/failed_ratio`）与阈值告警（`dispatch_max_failed_count/dispatch_max_failed_ratio`），默认读取 `data/state/signal_alert_ledger.csv`。
- 新增回归：`test_dispatch_alerts_missing_queue_returns_stable_summary_schema`、`test_build_baseline_daily_summary_collects_dispatch_metrics_from_ledger`。
- 本轮新增提交：
  - `cd6e008 fix: keep dispatch summary schema stable when queue is missing`
  - `752779e docs: update status for dispatch summary contract hotfix`
  - `55976f7 feat: add dispatch failure metrics to baseline summary`
  - `a1db96c test: cover baseline dispatch metrics and defaults`
- baseline-summary 增强：新增阈值告警字段 `alert_rules + alerts`（Signal 窗口信号数/胜率/平均收益 + 模拟累计收益）。
- baseline-summary 新增 `--emit-alert-queue` 与 `--alert-queue-file`，可导出 `outputs/baseline_summary_alerts.jsonl` 供派发链路复用。
- run-config 默认参数同步扩展 baseline-summary 新告警参数，并新增范围校验：`signal_min_win_rate ∈ [0,1]`。
- 新增回归：`test_build_baseline_daily_summary_generates_threshold_alerts`、`test_parse_config_task_rejects_invalid_signal_min_win_rate`，并扩展 baseline-summary CLI 子进程用例覆盖队列输出。
- 本轮定向验证：`pytest -q tests/test_baseline_summary.py tests/test_config_runner.py tests/test_cli_entry.py -k "baseline_summary or signal_min_win_rate"` => `5 passed`。
- 新增 baseline 告警独立模板：`examples/config.baseline.summary.alerts.json/.yaml`（单任务运行，默认导出告警队列）。
- 新增模板解析回归：`test_parse_config_task_supports_baseline_alerts_template`、`test_parse_config_task_supports_baseline_alerts_yaml_template`。
- 新增批处理模板：`examples/config.batch.baseline.alerts.daily.json/.yaml`，默认串联 `baseline-summary -> dispatch-alerts`（基线告警本地派发落盘）。
- 新增解析回归：`test_parse_config_tasks_supports_baseline_alerts_daily_batch_template`、`test_parse_config_tasks_supports_baseline_alerts_daily_yaml_batch_template`。
- 新增中长线策略 `dual_momentum`（短周期+长周期动量共振，叠加趋势/波动率过滤），并接入策略注册表与 CLI。
- `cli.py` 新增 `STRATEGY_CHOICES` 与 `COMPARE_DEFAULT_STRATEGIES` 常量，统一策略白名单，降低新增策略漏改风险。
- 新增模板：`examples/config.backtest.dual_momentum.json`、`examples/config.research.dual_momentum.json`，并更新 `examples/config.compare.json` 默认策略列表。
- 新增回归：`test_dual_momentum_signal_shape`、dual_momentum 模板解析测试、`compare` 默认策略子进程断言含 `dual_momentum`。
- 数据层新增 `DataManager.get_aligned_history`，提供多标的共同时间轴裁剪，减少组合链路重复对齐代码。
- `research/portfolio.py` 与 `cli.cmd_simulate_portfolio` 已接入统一对齐接口；若注入的 fake manager 缺少该方法，则自动回退到旧 `get_history + intersection` 路径。
- 新增回归：`test_get_aligned_history_returns_common_range`；组合回测/模拟相关测试全部通过。
- 按 TASK 主线继续开发：补齐数据层显式增量更新命令 `update-cache`（DataManager+CLI+模板+测试+README）。
- 模拟交易增强：`simulate` 资金曲线新增持仓快照列（side/qty/entry/mark/unrealized），summary 新增 `latest_position`。
- 模拟交易工件命名增强：`simulate` 新增 `--output-tag`，避免同策略多参数运行覆盖输出文件。
- 新增日常批处理模板：`examples/config.batch.refresh_mvp_daily.json`（`update-cache -> mvp -> report`）。
- 本轮定向验证通过：
  - `pytest -q tests/test_data_manager.py tests/test_config_runner.py tests/test_cli_entry.py -k "update_cache"`
  - `pytest -q tests/test_broker.py -k "allocation_per_signal_dynamic_quantity or cmd_simulate_can_resume_from_persisted_state"`
  - `pytest -q tests/test_config_runner.py tests/test_cli_entry.py -k "simulate_accepts_output_tag or simulate_cli_output_tag_avoids_overwrite_via_subprocess"`
  - `pytest -q tests/test_config_runner.py -k "refresh_mvp_daily_batch_template"`
