# Docker Compose 端口映射
BACKEND_PORT=8000
FRONTEND_PORT=8080

# 前端打包时注入 API 基础路径（Nginx 反向代理场景建议 /api）
VITE_API_BASE=/api

# 本地 npm run dev 使用时，可通过 Vite 代理转发至后端
VITE_BACKEND_TARGET=http://127.0.0.1:8000
